# 高尔夫江湖 — 游戏PK系统设计文档（V54 工作稿）

## 目录

1. 核心原则

2. 全程配置 / 规则模板 / 游戏实例的概念与关系

3. 全局配置

   3.1 公开范围

   3.2 全程洞序

   3.3 捐锅规则

   3.4 大风吹

4. 规则模板
   4.1 两人游戏规则模板
   4.1.1 比杆
   4.1.2 比洞
   4.1.3 两人8421
   4.1.4 三局
   4.1.5 油菜
   4.2 三人游戏规则模板
   4.2.1 斗大地主
   4.2.2 斗二地主
   4.2.3 斗小地主
   4.2.4 三人8421
   4.3 四人游戏规则模板
   4.3.1 拉丝
   4.3.2 四人8421
   4.3.3 固定三打一
   4.3.4 四人地主婆
   4.3.5 拉斯维加斯
   4.3.6 SKINS
   4.4 多人游戏规则模板
   4.4.1 大部队（任意人数 N）
   4.4.2 喇叭花（任意人数 N）

5. 游戏实例
   5.1 游戏实例的通用配置项
   5.2 游戏实例的特定配置（各游戏类型差异化参数）

6. 界面与交互
   6.1 三人动态玩法的逐洞阵营识别

7. 数据结构

8. 计算流程

------

## 1. 核心原则

### 1.1 游戏定义

游戏是两人或多人（两方/多方）之间，按照约定规则，依据每洞成绩计算的零和结果。系统仅计算和展示**积分**，不涉及货币换算。用户线下自行约定每积分对应的实际价值。

### 1.2 数据来源

- 用户只记录**真实杆数**。
- 系统根据让杆配置、奖励规则、顶洞规则、得分映射等，计算每洞每位球员的**积分损益**。
- **精度规则**：
  - 内部计算使用浮点数，保留小数点后1位（四舍五入）。
  - 显示时，若数值为整数，则显示整数；若为小数，则保留1位。
  - 让杆支持0.5倍数（特殊说明的除外，如8421、拉斯维加斯、大部队、喇叭花等）。

### 1.3 零和与捐锅

- 无捐锅时，同一游戏同一洞所有球员积分损益之和为0。
- 有捐锅时，球员积分之和与公共基金积分总额之和为0。

### 1.4 实时性

每录入一洞成绩或修改任何配置，立即重新计算所有受影响游戏的结果。

### 1.5 手工编辑

- 用户可手动修改表格中的积分值，系统进行零和校验（含捐锅）。
- 手工编辑后的单元格以特殊颜色标记，并提供“恢复系统计算值”按钮。

------

## 2. 全程配置 / 规则模板 / 游戏实例的概念与关系

### 2.1 全程配置（Global Settings）

- 指比赛（GAME）层级的全局设置，包括：全程洞序、公开范围、捐锅规则、大风吹。
- 这些配置影响本场比赛中的所有游戏，独立于具体游戏。

### 2.2 规则模板（Rule Template）

- 一种具体的游戏玩法类型及其所有可配置的参数集合（如奖励、顶洞、吃肉、扣分、包洞等）。
- 系统内置多种默认模板（如两人比杆、四人拉丝等），用户可修改参数后保存为自定义规则，存入“我的规则”列表。
- **用途**：规则模板用于**创建新游戏时提供初始规则**，并作为可复用模板供以后新建游戏使用。模板不是已有游戏的实时数据源。
- **保存逻辑**：
  - 用户可对“我的规则”列表中的规则模板进行参数配置，重新配置后，若修改了规则名称 → 保存为新规则，原规则不变。
  - 重新配置后，若名称不变 → 覆盖原规则（仅覆盖该模板本身，不自动改变已经创建的游戏实例）。

### 2.3 游戏实例（Game Instance）

- 基于“我的规则”中的某个规则模板，针对当前比赛（GAME）创建的具体游戏。
- 创建时，当前使用的规则形成该游戏**自己的实例规则快照**（创建时固化的实例规则），同时记录参与人员、有效洞范围、让杆/映射表、编队方式、排序等实例特有配置。
- 后续结算以游戏实例自己的规则为准，不再回头读取“我的规则”中的模板作为结算依据。
- 规则模板后续修改，**不自动改变**已经创建的游戏实例；已有实例不会因为“我的规则”变化而被动改变历史或未来结算规则。
- **主动修改**：只有用户明确对当前游戏执行规则修改，并确认将新规则应用到当前实例，当前实例规则才发生变化。

### 2.4 三者的关系

- 全程配置是比赛环境（洞序、捐锅、公开范围等），作用于本场比赛中的各游戏。
- 规则模板是玩法蓝图，只在**新建游戏**时提供初始规则。
- 游戏实例是实际运行的游戏；创建后拥有自己的实例规则快照，结算结合**全程配置 + 该实例自己的规则 + 该实例的人员/让杆等配置**。
- 不得把“我的规则”理解为已有实例的实时引用：改模板不等于改已开局的游戏。

------

## 3. 全局配置

全局配置是在比赛（GAME）层级设置的参数，影响本场比赛中的所有游戏。用户在比赛详情页的“全局设置”区域进行配置，当尚未添加任何游戏时，此区域置灰不可编辑。配置项包括：公开范围、全程洞序、捐锅规则、大风吹。

### 3.1 公开范围

| 选项         | 说明                                         |
|:---------- |:------------------------------------------ |
| `完全公开`（默认） | 所有用户（包括非本GAME参与者）均可见游戏结果。                  |
| `仅本组可见`    | 仅本组（四人组）球员可见该游戏的TAB结果。                     |
| `仅本赛事可见`   | 本GAME内所有组的球员均可见该游戏结果（**仅当比赛存在多个组时显示此选项**）。 |

- **默认值**：`完全公开`。
- **动态选项**：当比赛只有一组时，“仅本赛事可见”选项不显示；当存在多组时，显示全部三个选项。

### 3.2 全程洞序

- **定义**：用户自定义实际打球的洞号顺序，例如 `[3,4,5,...,18,1,2]` 表示从第3洞开始打到第2洞结束。
- **默认值**：`[1,2,3,...,18]`。按照用户选择半场的顺序默认，比如用户前9选B场，后9选A场，则默认值为【B1,B2......B9,A1,A2......A9]。
- **影响**：所有游戏的计算均基于此洞序的索引（0‑based）。用户可随时修改洞序，修改后系统自动清除所有游戏的手工编辑记录，并按新洞序重新计算所有游戏的历史结果。
- **存储**：以JSON数组格式保存在比赛表中。

### 3.3 捐锅规则

**捐锅**是高球赌局中的一种常见玩法，指从每洞胜者（或指定球员）的得分中提取一定数量的分数，集中存入一个“公共基金”，用于支付球费、小费等。系统仅计算基金总额及每人应付积分，不涉及真实货币。

捐锅规则是全局配置，可关联一个或多个游戏。共四种类型，用户可择一使用。

#### 3.3.1 类型1：不捐锅（默认值）

- 不抽取任何分数，无公共基金。

#### 3.3.2 类型2：胜方每洞捐N积分，总共捐M积分

- **参数**：`donation_per_hole` (N), `total_cap` (M)，N、M为整数或0.5倍数，M ≥ N。
- **规则**：
  - 每洞**所有净损益为正的球员**（胜者）每人捐出固定 N 积分。
  - 系统维护一个累计捐出总额 S（初始为0）。
  - 设该洞胜者人数为 W。理论捐出额 = W × N。
  - 若 S + W×N ≤ M，则每人捐 N 分，S 增加 W×N。
  - 若 S + W×N > M，则本洞总捐出额 = M - S，每个胜者捐出 (M - S) / W（保留1位小数），捐锅结束。
- **底部显示**：每人累计捐锅分数（正数）。

#### 3.3.3 类型3：全捐，捐到M积分为止

- **参数**：`total_cap` (M)。
- **规则**：
  - 每洞所有胜者捐出其本洞**全部正分**（即胜者赢得的分数）。
  - 累计捐出总额 ≤ M。当剩余额度 R = M - S_curr 小于本洞胜者总赢分时，按比例分配剩余额度（每人捐出 R × (个人赢分 / 本洞总赢分)）。
  - 达到 M 后停止。
- **底部显示**：每人累计捐锅分数（正数）。

#### 3.3.4 类型4：大锅饭

- **参数**：`total_fund` S（积分）。
- **规则**：
  - 每洞所有胜者捐出其全部正分，因此胜者本洞得分显示为0。
  - 系统累计每位球员的净损益（得分）。
  - 比赛过程中（每洞后），动态计算底部两行：
    - 第一行：应付分数 = 球员累计净损益（仅负值，正值显示0）。
    - 第二行：应付积分 = 应付分数绝对值 × (S / 所有应付分数绝对值之和)。若所有应付分数为0，则每人应付 = S / 参与人数。
- **基金总额**始终为 S 积分。
- 当某游戏被关联为”大锅饭“捐锅方式的游戏时，此游戏的”积分“概念被转化为”得分“概念，因为此分对应的实际积分价值是随着打球进展而有不断变小的趋势。

#### 3.3.5 捐锅适用游戏选择

- 当比赛中**仅存在一个游戏**时，系统自动将该游戏设为捐锅规则的适用游戏（`applicable_game_ids` 默认为该游戏ID）。
- 当**存在多个游戏**时，用户必须手动勾选一个或多个游戏作为捐锅规则的适用对象。若用户未勾选任何游戏，系统提示“请至少选择一个游戏”。
- 若某个被关联的游戏被删除，系统自动从规则的适用游戏列表中移除该游戏ID；若规则不再适用任何游戏，则自动禁用该规则（或提示用户删除）。

### 3.4 大风吹

- **类型**：布尔值（启用/禁用）。
- **默认值**：`false`。
- **作用**：启用后，所有**支持顶洞机制的游戏**（如比洞、拉丝、8421、固定三打一、斗地主、喇叭花等）的正常顶洞吃肉规则被改变：
  - 在整个比赛过程中，顶洞时正常累积肉块，但**正常吃肉（非顶洞时胜队吃肉）仍然照常执行**。
  - 在**全程洞序的最后一洞**结束后，系统额外执行一次“大风吹”：
    - 若最后一洞有胜队（或喇叭花游戏中有胜者），则将该游戏当前累积的所有剩余肉块一次性分配给胜队（或所有胜者），分配比例同该游戏的正常吃肉规则。
    - 若最后一洞为平局，则所有剩余肉块作废（清零）。
- **注意**：大部队游戏不支持大风吹（即此开关对大部队无效）。

------

## 4. 规则模板（含玩法说明与算法示例）

### 4.1 两人游戏规则模板

#### 4.1.1 比杆

**玩法说明**
每洞直接比较双方**真实杆数**，杆差乘以每杆分值得到该洞损益。**让杆为总杆让杆**：为每个选中的组合设置一个整数让杆值 `N`（可为负数）。

- 若 `N > 0`：表示“前者让后者 N 杆”，即前者的初始总分 -N×每杆分值，后者的初始总分 +N×每杆分值。
- 若 `N < 0`：表示“后者让前者 |N| 杆”，即后者的初始总分 -|N|×每杆分值，前者的初始总分 +|N|×每杆分值。
- 若 `N = 0`：无让杆。
  累计总分 = 初始分 + 所有洞损益之和。支持奖励（加法/乘法）。

**规则模板参数**（保存在“我的规则”中）

| 参数    | 说明              | 默认值                                    |
|:----- |:--------------- |:-------------------------------------- |
| 奖励模式  | 无 / 加法 / 乘法     | 无                                      |
| 奖励配置表 | 小鸟、老鹰等对应的奖励值/倍数 | 小鸟+1(加法)或×2(乘法)，老鹰+3或×5，信天翁/HIO+10或×10 |

**游戏实例配置项**（创建具体游戏时填写）

| 配置项               | 说明                           | 默认值   |
|:----------------- |:---------------------------- |:----- |
| 游戏名称              | 自定义标识                        | “比杆赛” |
| **每杆分值**          | 每杆对应积分                       | 1     |
| **选择1V1对阵组合**     | 列出本组所有可能的两人组合，提供复选框，最少选一个组合。 | 全选    |
| **有效洞范围**         | 基于全程洞序的起始/结束索引               | 全洞    |
| **为每个选中的组合设置让杆值** | 每个组合有一个数值输入框（整数，可为负数），表示让杆数  | 0     |

**算法示例**

- 本组球员：A、B、C、D。用户勾选A-B、C-D两个组合，并设置让杆值：A-B = 2（A让B 2杆），C-D = -1（表示D让C 1杆）。每杆分值=1，奖励模式=乘法（小鸟×2）。
- 第1洞（par4）：实际杆数 A=4,B=3,C=5,D=4。
  - 组合A-B：初始分 A=-2，B=+2。杆差=4-3=1（B胜）。B打小鸟，奖励×2，调整杆差=2。本洞积分 B=+2，A=-2。累计分 A=-2-2=-4，B=+2+2=+4。
  - 组合C-D：让杆值-1表示D让C 1杆 → 初始分 C=+1，D=-1。杆差=5-4=1（D胜）。D打PAR无奖励，杆差=1。本洞积分 D=+1，C=-1。累计分 C=+1-1=0，D=-1+1=0。
- 最终总积分：A=-4，B=+4，C=0，D=0。

#### 4.1.2 比洞

**玩法说明**
每洞比较**调整后杆数**（实际杆数 - 逐洞让杆），胜者得 +1 胜洞，负者 -1，平局0。支持顶洞（平局时累积肉块，后续胜者吃肉）和乘法奖励（仅对胜者生效，使胜洞差乘以倍数）。每洞积分 = 胜洞差 × 每洞分值。
**让杆为逐洞让杆**：为每个选中的组合设置一个让杆值（支持0.5倍数，可为负数）。让杆方向同比杆：正值表示前者让后者，负值表示后者让前者。调整方式：让杆方的杆数不变，受让方的杆数减去让杆绝对值。即：

- 若 `N > 0`：前者让后者 N 杆 → 后者调整后杆数 = 实际杆数 - N，前者不变。
- 若 `N < 0`：后者让前者 |N| 杆 → 前者调整后杆数 = 实际杆数 - |N|，后者不变。
- 若 `N = 0`：双方杆数不变。

**规则模板参数**（保存在“我的规则”中）

| 参数      | 说明               | 默认值                                      |
|:------- |:---------------- |:---------------------------------------- |
| 奖励模式    | 无 / 乘法           | **乘法奖励**                                 |
| 乘法奖励配置表 | 成绩对应倍数           | PAR×1，小鸟×2，老鹰×5，信天翁/HIO×10               |
| 顶洞定义    | 得分打平 / 无顶洞       | **得分打平**                                 |
| 吃肉数量对应表 | 成绩类别 → 吃肉数量（0~3） | 比PAR更差=0，PAR=1，小鸟=2，老鹰=3，信天翁/HIO及比老鹰更好=3 |
| 肉的分值类型  | 肉不含奖励 / 肉包含奖励    | **肉不含奖励**                                |

**游戏实例配置项**

| 配置项               | 说明                               | 默认值   |
|:----------------- |:-------------------------------- |:----- |
| 游戏名称              | 自定义标识                            | “比洞赛” |
| **每洞分值**          | 每胜洞对应积分                          | 1     |
| **选择1V1对阵组合**     | 列出本组所有可能的两人组合，提供复选框，最少选一个组合。     | 全选    |
| **有效洞范围**         | 基于全程洞序的起始/结束索引                   | 全洞    |
| **为每个选中的组合设置让杆值** | 每个组合有一个数值输入框（支持0.5倍数，可为负数），表示让杆数 | 0     |

**算法示例**

- 组合A-B：让杆值 = 0.5（A让B 0.5杆）。A实际4，B实际5 → 调整后 A=4，B=4.5 → A胜，胜洞差=1，无奖励，本洞积分 A=+1，B=-1。
- 组合C-D：让杆值 = -1（D让C 1杆）。C实际5，D实际4 → 调整后 C=5-1=4，D=4 → 平局，本洞积分0，累积肉块+1。

#### 4.1.3 两人8421

**玩法说明**
每洞根据实际杆数与标准杆的差值（diff）查询用户自定义的成绩-得分映射表，得到个人得分。双方得分差乘以每分价值即为该洞积分。支持扣分（根据扣分配置项参数扣分）、顶洞、吃肉（含连续翻倍等）。**无让杆**。

**规则模板参数**（保存在“我的规则”中）

| 参数      | 说明                                         | 默认值                      |
|:------- |:------------------------------------------ |:------------------------ |
| 扣分触发条件  | 从+N(N默认=4）开始 / 从DOUBLEPAR+N（N默认=0）开始 / 不扣分 | **从+4开始扣分**              |
| 扣分上限    | 不封顶 / 扣N（N默认=2）分封顶                         | **扣2分封顶**                |
| 顶洞定义    | 得分打平 / 得分≤N（N默认=1） / 无顶洞                   | **得分打平**                 |
| 吃肉数量对应表 | 成绩类别 → 吃肉数量（0~3或“全部”）                      | **所有成绩均对应1块肉**           |
| 肉的分值类型  | 肉算N（N默认=2）分 / 分值翻倍 / 一次全吃连续翻倍              | **分值翻倍：本洞得N分，则该块肉按N分计算** |
| 肉值封顶    | 不封顶 / N（N默认=3）分封顶                          | **不封顶**                  |

**默认扣分阶梯（最终确认）**：`diff=+4 → -1分`；`diff≥+5 → -2分`，其中 `+6、+7……` 均保持 `-2分`，不再继续扣分。

**“分值翻倍”默认语义（最终确认）**：每次吃肉默认吃1块；该块肉的分值等于胜方本洞实际游戏得分 N，即“本洞得 N 分，肉就算 N 分”，肉值不封顶。这里不是固定1分，也不是按 1→2→4→8 的独立连续翻倍序列。

**注**  当顶洞选择“得分<=N"时，球员得分正常，但**累积一块肉**

**游戏实例配置项**（创建具体游戏时填写）

| 配置项           | 说明                                                                                                | 默认值                       |
|:------------- |:------------------------------------------------------------------------------------------------- |:------------------------- |
| 游戏名称          | 自定义标识                                                                                             | “8421”                    |
| **每分价值**      | 得分映射表中1分对应积分                                                                                      | 1                         |
| **成绩-得分映射表**  | 用户可自定义，需提供 diff 范围对应的得分（例如 diff ≤ -3 得 32，-2 得 16，-1 得 8，0 得 4，+1 得 2，+2 得 1，+3 得 0；≥ +4 由扣分规则决定） | 默认表（见“成绩-得分映射表配置说明”默认值表格） |
| **选择1V1对阵组合** | 列出本组所有可能的两人组合，提供复选框，可多选，最少选一个组合。                                                                  | 全选                        |
| **有效洞范围**     | 基于全程洞序的起始/结束索引                                                                                    | 全洞***                     |

**成绩-得分映射表配置说明**

- **可配置范围**：`diff = -3, -2, -1, 0, +1, +2, +3` 七个档位。

- **配置方式**：每个档位使用滑轮选择，数值为 **非负整数**（≥0）。

- **默认值**：

  | diff | 得分  |
  |:---- |:--- |
  | ≤ -3 | 32  |
  | -2   | 16  |
  | -1   | 8   |
  | 0    | 4   |
  | +1   | 2   |
  | +2   | 1   |
  | +3   | 0   |

- **未定义区域（diff ≥ +4）**：用户无法自定义得分。系统根据**扣分规则**处理：

  - 若扣分规则启用（例如“从 +N 开始”），则按扣分规则计算得分（可为负或零）。
  - 若扣分规则设为“不扣分”，则得分为 0。

**优先级说明**：

- 系统首先检查 diff 是否在 -3 到 +3 范围内：
  - 若是且映射得分 > 0，则使用映射得分。
  - 若是但映射得分 = 0，则进入扣分规则（若启用）。
  - 若否（diff ≥ +4），则进入扣分规则（若启用），否则得 0。

**算法示例**

- 本组球员：A、B、C、D。用户勾选A-B和C-D两个组合。每分价值=1，默认得分映射表，顶洞定义=得分打平，扣分从+4开始（+4扣1分，+5及以上扣2分，扣2分封顶），吃肉数量对应表默认值。
- 第1洞（par4）：实际杆数 A=3, B=5, C=6, D=4。
  - 组合A-B：A diff=-1 → 得分8，B diff=+1 → 得分2，分差=6，A胜，本洞积分 A=+6，B=-6。
  - 组合C-D：C diff=+2 → 得分1，D diff=0 → 得分4，分差=3，D胜，本洞积分 D=+3，C=-3。
- 第2洞（par4）：A=8, B=4, C=5, D=5。
  - 组合A-B：A diff=+4 → 进入扣分规则（从 +4 开始扣分，2 分封顶）：+4 扣 1 分 → 得分-1；B diff=0 → 得分4；分差=4-(-1)=5，B胜，本洞积分 B=+5，A=-5。
  - 组合C-D：C diff=+1 → 得分2，D diff=+1 → 得分2，平局 → 本洞积分0，累积肉块+1。
- 第3洞（par4）：A=4, B=4, C=3, D=4。
  - 组合A-B：平局 → 顶洞，积分0，累积肉块+1（目前累积2）。
  - 组合C-D：C diff=-1 → 得分8，D diff=0 → 得分4，分差=4，C胜，且此时累积肉块=2 → 胜者C吃肉：两人8421默认所有成绩每次吃1块肉，因此从当前累积2块中吃1块。C本洞个人8421得分为8，所以按“分值翻倍”的最终确认语义，该块肉价值8分。额外8分计入本洞积分；剩余肉块继续保留。
- 最终每名球员的总积分 = 所有包含他的组合积分之和。例如A的积分来自组合A-B：-5（第2洞）+（其他洞累加）等。

#### 4.1.4 三局

**玩法说明**
三局游戏将18洞分为前九、后九、全场三段，每段比较双方调整后总杆（实际总杆 - 段让杆），低者赢得该段（+1），高者输（-1），平局0。球员总积分 = 三段积分之和。**有效洞范围不可选择**（必须全程）。支持个人段让杆（每位球员独立设置前九、后九、全场让杆值）。

**规则模板参数**（保存在“我的规则”中）
无（三局游戏没有可复用的玩法参数，所有参数均在实例中配置）

**游戏实例配置项**（创建具体游戏时填写）

| 配置项                 | 说明                                | 默认值  |
|:------------------- |:--------------------------------- |:---- |
| 游戏名称                | 自定义标识                             | “三局” |
| **每局分值**            | 前九、后九、全场获胜所获得的积分，可分别设置            | 均为1  |
| **选择1V1对阵组合**       | 列出本组所有可能的两人组合，提供复选框，可多选，最少选1个组合。  | 全选   |
| **为每个选中的组合设置三段让杆值** | 每个组合有三个输入框（前九、后九、全场），支持0.5倍数，可为负数 | 0    |

**算法示例**

- 本组球员：A、B、C、D。用户勾选A-B和C-D两个组合。每局分值：前九=1，后九=1，全场=1。个人段让杆：A前九=0，后九=1，全场=1；B全0；C全0；D前九=1，后九=0，全场=0。
- 前九洞（1-9）：实际总杆 A=42，B=43，C=45，D=46。
  - 组合A-B：调整后 A=42，B=43 → A胜 → 前九积分 A=+1，B=-1。
  - 组合C-D：调整后 C=45，D=46-1=45 → 平局 → 前九积分 0。
- 后九洞（10-18）：实际总杆 A=44，B=43，C=42，D=44。
  - 组合A-B：调整后 A=44-1=43，B=43 → 平局 → 后九积分 0。
  - 组合C-D：调整后 C=42，D=44 → C胜 → 后九积分 C=+1，D=-1。
- 全场（1-18）：实际总杆 A=86，B=86，C=87，D=90。
  - 组合A-B：调整后 A=86-1=85，B=86 → A胜 → 全场积分 A=+1，B=-1。
  - 组合C-D：调整后 C=87，D=90-1=89 → C胜 → 全场积分 C=+1，D=-1。
- 每名球员总积分：A=+2，B=-2，C=+2，D=-2。

**注意**：

- 三局游戏的有效洞范围固定为全程，不可选择部分洞。

#### 4.1.5 油菜

**玩法说明**
油菜是两人比洞的简化版：每洞比较调整后杆数，胜者得 +1 胜洞，负者 -1，平局0。**无奖励、无顶洞、无吃肉**。每洞积分 = 胜洞差 × 每洞分值。
**让杆为组合级逐洞让杆**：为每个选中的1V1组合设置一个让杆值（支持0.5倍数，可为负数）。让杆方向：正值表示“前者让后者”，即后者的实际杆数减去该值（受让），前者不变；负值表示“后者让前者”，即前者的实际杆数减去该值的绝对值，后者不变。

**规则模板参数**（保存在“我的规则”中）
无（油菜游戏没有可复用的玩法参数，所有参数均在实例中配置）

**游戏实例配置项**（创建具体游戏时填写）

| 配置项               | 说明                                 | 默认值  |
|:----------------- |:---------------------------------- |:---- |
| 游戏名称              | 自定义标识                              | “油菜” |
| **每洞分值**          | 每胜洞对应积分                            | 1    |
| **选择1V1对阵组合**     | 列出本组所有可能的两人组合，提供复选框，可多选，最少选一个组合。   | 全选   |
| **有效洞范围**         | 基于全程洞序的起始/结束索引                     | 全洞   |
| **为每个选中的组合设置让杆值** | 每个组合有一个数值输入框（支持0.5倍数，可为负数），表示逐洞让杆数 | 0    |

**算法示例**

- 本组球员：A、B、C、D。用户勾选A-B和C-D两个组合。每洞分值=1。让杆设置：A-B = 0.5（A让B 0.5杆），C-D = -1（D让C 1杆）。
- 第1洞（par4）：实际杆数 A=4, B=5, C=5, D=4。
  - 组合A-B：让杆0.5 → B受让，B调整后 = 5-0.5 = 4.5，A不变=4 → A胜，胜洞差=1 → 本洞积分 A=+1，B=-1。
  - 组合C-D：让杆-1 → D让C 1杆，C受让，C调整后 = 5-1 = 4，D不变=4 → 平局 → 积分0。
- 第2洞：A=3, B=4, C=6, D=4。
  - 组合A-B：A=3, B=4-0.5=3.5 → A胜，积分 A=+1，B=-1。
  - 组合C-D：C=6-1=5, D=4 → D胜，胜洞差=1 → 积分 D=+1，C=-1。
- 累计积分（假设只有这两洞）：
  - A：来自A-B：+1+1=+2
  - B：来自A-B：-1-1=-2
  - C：来自C-D：0-1=-1
  - D：来自C-D：0+1=+1

**注意**：

- 让杆为逐洞应用，每洞独立。
- 有效洞范围可任意选择（基于全程洞序索引）。
- 每洞分值在实例中直接配置，无需规则模板。

### 4.2 三人游戏规则模板

#### 4.2.1 斗大地主

**玩法说明**
三人斗大地主是三人游戏，分为**单人队（A）** 与**双人队（B、C）**。每洞比较 A（第1名） 的调整后杆数与 B、C （2/3名）中**最低调整后杆数**，杆数低者胜。

- 积分系数 K（每点对应积分，游戏实例中配置）
- 积分分配：
  - A 胜：A +2K，B、C 各 -K
  - BC 胜：A -2K，B、C 各 +K
  - 打平：所有球员 0 分
- 支持乘法奖励、顶洞、吃肉（成绩-吃肉数量对应表），**无包洞**。
- **让杆为个人逐洞让杆**：为每位球员独立配置按PAR3/4/5的让杆值（支持0.5倍数，可负）。每洞调整后杆数 = 实际杆数 - 个人让杆值。

**规则模板参数**（保存在“我的规则”中）

| 参数            | 说明                    | 默认值                        |
|:------------- |:--------------------- |:-------------------------- |
| 奖励模式          | 无 / 乘法                | 无                          |
| 乘法奖励配置表       | 成绩对应倍数                | 小鸟×2，老鹰×5，信天翁/HIO×10，PAR×1 |
| 顶洞定义          | 得分打平 / 无顶洞            | 得分打平                       |
| **顶洞时是否重新排序** | 是 / 否（即顶洞时是否根据成绩调整组合） | 否（顶洞不换组合）                  |
| 吃肉数量对应表       | 成绩类别 → 吃肉数量（0~3或“全部”） | 比鸟更好=全部，鸟=2，PAR=1，比PAR更差=0 |
| 肉的分值类型        | 肉不含奖励 / 肉包含奖励         | 肉不含奖励                      |

**游戏实例配置项**（创建具体游戏时填写）

| 配置项                        | 说明                                                                                                                                                      | 默认值     |
|:-------------------------- |:------------------------------------------------------------------------------------------------------------------------------------------------------- |:------- |
| 游戏名称                       | 自定义标识                                                                                                                                                   | “斗大地主”  |
| **积分系数K**                  | 每点对应积分                                                                                                                                                  | 1       |
| **选择球员**                   | 从本组球员列表中勾选三人                                                                                                                                            | —       |
| **有效洞范围**                  | 基于全程洞序的起始/结束索引                                                                                                                                          | 全洞      |
| **分组方式**                   | 固斗 / 乱斗 / 高手不见面                                                                                                                                         | 固斗      |
| **初始排序方式**                 | 随机 / 手工指定 / 按差点排序（低到高）。该排序始终显示在配置界面，用户可随时调整，不依赖分组方式                                                                                                     | 随机      |
| **排序规则**（分组方式为乱斗或高手不见面时显示） | 当两人调整后杆数相同时，按以下规则决定顺序： • **按出身成绩排序**：比较上一洞的调整后杆数，杆数低的排前；若仍相同，继续向前追溯，直到分出先后。 • **按输赢排序**：比较上一洞的个人积分（赢为正，输为负），积分高的排前（即赢家在前，输家在后）；若积分相同，则按上一洞积分排名（排名高的在前）。 | 按出身成绩排序 |
| **个人让杆值**                  | 为每位球员分别设置按PAR3/4/5的让杆值（支持0.5倍数，可负）                                                                                                                      | 0       |

#### **各分组方式详解**

#### 1. 固斗

- 初始排序（通过初始排序方式确定）仅用于第一洞前的顺序，之后**固定不变**。
- 每洞均以**排序中的第一名**为单人队（A），**第二名和第三名**为双人队（B、C）。
- 后续洞不再重新排序（不受顶洞选项影响）。

#### 2. 乱斗

- 初始排序用于第一洞前的顺序。
- 每洞结束后，根据规则模板中的“顶洞时是否重新排序”决定：
  - 若选项为**“否”**（顶洞不换组合）：当本洞为顶洞时，排序保持不变；非顶洞时，根据本洞调整后杆数从低到高重新排序（杆数低者排前，相同则按“排序规则”处理）。
  - 若选项为**“是”**（顶洞也按成绩调整组合）：无论是否顶洞，每洞结束后都根据本洞调整后杆数重新排序（顶洞时也按杆数排序，相同按“排序规则”处理）。
- 下一洞按新排序，仍以第一名作为单人队（A），后两名作为双人队（B、C）。

#### 3. 高手不见面

- **适用前提**：三人中，前两名定义为“高手”，第三名为“低手”。
- 初始排序（通过初始排序方式确定）中的前两名为高手，第三名为低手。该排序作为第一洞前的顺序。
- **第一洞**：以排序中的**第一名**为单人队（A），**第二名和第三名**为双人队（B、C）。
- **每洞后的排序规则**：
  - 仅对前两名（高手）进行重新排序。根据规则模板中的“顶洞时是否重新排序”决定：
    - 若选项为**“否”**（顶洞不换组合）：当本洞为顶洞时，前两名顺序不变；非顶洞时，比较他们本洞的调整后杆数，杆数低者排第一，高者排第二。若杆数相同，按“排序规则”处理。
    - 若选项为**“是”**（顶洞也按成绩调整组合）：无论是否顶洞，每洞结束后都比较前两名的调整后杆数，重新排序（顶洞时也重新排，相同按“排序规则”处理）。
  - 第三名（低手）**永远固定在第三位**，不参与排序变化。
- **下一洞**：按新排序，第一名仍为单人队（A），后两名（第二和第三）为双人队（B、C）。

**排序规则说明**

- **按成绩排序**：如果本洞调整后杆数相同，比较上一洞的调整后杆数，杆数低者排前；若相同，继续向前追溯，直到上一洞、上上一洞……直至分出先后。这种方式保证了历史成绩的连贯性。
- **按输赢排序**：如果本洞调整后杆数相同，则应比较本洞的积分，本洞积分多者排前。如果本洞积分也相同，则比较上一洞的个人积分（赢为正，输为负），积分高者排前......直至分出先后。

**注意**：

- 个人让杆值按PAR3/4/5配置，每洞根据标准杆选择对应的让杆值。
- 有效洞范围可选。
- 无包洞配置。

#### 4.2.2 斗二地主

**玩法说明**
三人斗二地主是三人游戏，分为**单人队（A）** 与**双人队（B、C）**。每洞比较**第2名（排序中间）** 的调整后杆数与**第1名和第3名的平均杆数** `(第1名+第3名)/2`（保留一位小数），杆数低者胜。

- 积分系数 K（每点对应积分，游戏实例中配置）
- 积分分配：
  - 单人队胜：A +2K，B、C 各 -K
  - 双人队胜：A -2K，B、C 各 +K
  - 打平：所有球员 0 分
- 支持乘法奖励、顶洞、吃肉（成绩-吃肉数量对应表），**支持包洞**（仅对双人队有效）。
- **让杆为个人逐洞让杆**：为每位球员独立配置按PAR3/4/5的让杆值（支持0.5倍数，可负）。每洞调整后杆数 = 实际杆数 - 个人让杆值。
- **排序原则（乱斗）**：每洞结束后都执行**正常排序**：按本洞调整后（adjusted）杆数从低到高排序；杆数相同按现有「排序规则」处理。**PUSH / 顶洞也正常 rerank**。不存在 PUSH 专用 freeze、不存在组合锁定、不存在红蓝三角特殊处理。
- **下一洞身份**：始终按当洞排序结果：第2名 = 单人队；第1名、第3名 = 双人队。
- **实现原则（UI）**：单人队/双人队身份及红蓝三角，始终根据当前排序结果渲染。不为 PUSH 增加单独的身份冻结或显示逻辑。
- **算法说明（为何顶洞后组合通常不变）**：打平条件为「第2名 adjusted 杆数 = (第1名 adjusted 杆数 + 第3名 adjusted 杆数) / 2」。三人按 adjusted 杆数从低到高排序时，第2名天然是中间值。因此发生顶洞后再执行正常排序：原第2名仍处于中间位置，单人队仍是该球员，另外两人仍组成双人队。故「顶洞后组合不变」是正常排序算法自然得到的结果，**不是**「顶洞不排序 / 顶洞冻结顺序 / keep-combination / 特殊锁队」，也**不要**再把它写成独立业务规则。
- **顶洞与普通洞**：使用同一套正常排序流程。由于上述打平比较式，第2名在顶洞时仍自然保持中间位置，因此单人队/双人队组合通常保持不变，无需额外冻结排序。

**规则模板参数**（保存在“我的规则”中）

| 参数                          | 说明                                                                           | 默认值                        |
|:--------------------------- |:---------------------------------------------------------------------------- |:-------------------------- |
| 奖励模式                        | 无 / 乘法                                                                       | **乘法奖励**                   |
| 乘法奖励配置表                     | 成绩对应倍数                                                                       | 小鸟×2，老鹰×5，信天翁/HIO×10，PAR×1 |
| 顶洞定义                        | 得分打平 / 无顶洞                                                                   | 得分打平                       |
| 吃肉数量对应表                     | 成绩类别 → 吃肉数量（0~3或“全部”）                                                        | 比鸟更好=全部，鸟=2，PAR=1，比PAR更差=0 |
| 肉的分值类型                      | 肉不含奖励 / 肉包含奖励                                                                | 肉不含奖励                      |
| **包洞触发条件**（四选一，默认“不包洞”）     | 1. 不包洞 2. 从 +N 开始包洞（N默认4） 3. 从 DOUBLEPAR + N 开始包洞（N默认0） 4. 与同伴杆差 N 杆包洞（N默认3） | 不包洞                        |
| **包洞前置条件**（仅当触发条件非“不包洞”时可选） | 1. 同伴顶头才包洞（同伴杆数 ≤ 对方最好成绩） 2. 与同伴成绩无关                                         | 与同伴成绩无关                    |

**游戏实例配置项**（创建具体游戏时填写）

| 配置项                  | 说明                                                                                                                                                                          | 默认值     |
|:-------------------- |:--------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |:------- |
| 游戏名称                 | 自定义标识                                                                                                                                                                       | “斗二地主”  |
| **积分系数 K**           | 每点对应积分                                                                                                                                                                      | 1       |
| **选择球员**             | 从本组球员列表中勾选三人                                                                                                                                                                | —       |
| **有效洞范围**            | 基于全程洞序的起始/结束索引                                                                                                                                                              | 全洞      |
| **分组方式**             | 固斗 / 乱斗                                                                                                                                                                     | 乱斗      |
| **初始排序方式**           | 随机 / 手工指定 / 按差点排序（低到高）。该排序始终显示在配置界面，用户可随时调整，不依赖分组方式                                                                                                                         | 随机      |
| **排序规则**（分组方式为乱斗时显示） | 当两人调整后杆数相同时，按以下规则决定顺序： • **按出身成绩排序**：比较上一洞的调整后杆数，杆数低的排前；若仍相同，继续向前追溯，直到分出先后。 • **按输赢排序**：首先比较**本洞个人积分**（赢为正，输为负，平为0），积分高的排前；若本洞积分相同，则比较上一洞的个人积分（赢为正，输为负），积分高的排前；若仍相同，继续向前追溯。 | 按出身成绩排序 |
| **个人让杆值**            | 为每位球员分别设置按PAR3/4/5的让杆值（支持0.5倍数，可负）                                                                                                                                          | 0       |

#### 各分组方式详解

#### 1. 固斗

- 初始排序（通过初始排序方式确定）仅用于第一洞前的顺序，之后**固定不变**。
- 每洞均以**排序中的第2名**为单人队（A），**第1名和第3名**为双人队（B、C）。
- 后续洞不再重新排序（不受顶洞影响）。

#### 2. 乱斗

- 初始排序用于第一洞前的顺序。
- 每洞结束后，无论是否顶洞，都按本洞调整后杆数正常重新排序（PUSH 洞也正常 rerank）。杆数相同按「排序规则」处理。
- 下一洞始终按照排序结果：第2名 = 单人队，第1名和第3名 = 双人队。
- 不存在 PUSH 专用 freeze，不存在组合锁定。顶洞后组合通常不变，是打平比较式下第2名仍为中间值的数学结果，不是特殊锁队规则。

#### 包洞规则详解（仅适用于双人队）

- **触发条件**：根据规则模板中选定的条件，判断双人队中哪位球员（第1名或第3名）的成绩达到了包洞标准。
- **前置条件**：
  - “同伴顶头才包洞”：只有当同伴的杆数 ≤ 对方最好成绩时，才执行包洞。
  - “与同伴成绩无关”：只要触发条件满足，就执行包洞。
- **执行规则**：
  - 若双人队中仅一人触发包洞，则该球员承担本队该洞的**所有正常负分**（即队友得0分），肉的损失各自承担（不转嫁）。
  - 若两人均触发包洞，则恢复正常平分（每人承担一半负分）。

**注意**：

- 个人让杆值按PAR3/4/5配置，每洞根据标准杆选择对应的让杆值。
- 有效洞范围可选。
- 无“高手不见面”分组方式。
- 顶洞与普通洞使用同一套正常排序流程，不设置特殊冻结逻辑。若正常排序后单人/双人组合未发生变化，这是排序结果本身（第2名仍为中间值），而不是额外的顶洞规则。

#### 4.2.3 斗小地主

**玩法说明**
三人斗小地主是三人游戏，分为**单人队（A）** 与**双人队（B、C）**。根据排序，**第三名**为单人队（A），**第一名和第二名**为双人队（B、C）。每洞比较 A 的调整后杆数与 B、C 中**最高杆数**（即双人队的最差成绩），杆数低者胜。

- 积分系数 K（每点对应积分，游戏实例中配置）
- 积分分配：
  - A 胜：A +2K，B、C 各 -K
  - BC 胜：A -2K，B、C 各 +K
  - 打平：所有球员 0 分
- 支持乘法奖励、顶洞、吃肉（**无需成绩条件，每次获胜自动吃 1 块肉**），**支持包洞**（仅对双人队有效，有前置条件）。
- **顶洞与肉池**：当满足顶洞条件时，本洞积分为 0，**肉池增加 1 块肉**。肉池可跨洞累计。后续每次产生胜负时，胜方自动从肉池中**最多吃 1 块肉**；若肉池仍有剩余，剩余肉继续保留，等待之后的胜负洞继续结算。
  - 示例：连续顶洞 2 次后肉池为 2；下一次出现胜负时只吃 1 块，肉池仍剩 1 块，再由后续胜负继续吃。
- **让杆为个人逐洞让杆**：为每位球员独立配置按PAR3/4/5的让杆值（支持0.5倍数，可负）。每洞调整后杆数 = 实际杆数 - 个人让杆值。
- **顶洞时组合调整**：可配置（规则模板参数“顶洞时是否重新排序”），默认否。该配置仅作用于斗小地主乱斗，与斗二地主、三人8421 的顶洞排序规则相互独立。

**规则模板参数**（保存在“我的规则”中）

| 参数                          | 说明                                                                           | 默认值                        |
|:--------------------------- |:---------------------------------------------------------------------------- |:-------------------------- |
| 奖励模式                        | 无 / 乘法                                                                       | 无                          |
| 乘法奖励配置表                     | 成绩对应倍数                                                                       | 小鸟×2，老鹰×5，信天翁/HIO×10，PAR×1 |
| 顶洞定义                        | 得分打平 / 无顶洞                                                                   | 得分打平                       |
| **顶洞时是否重新排序**               | 是 / 否（即顶洞时是否根据成绩调整组合）                                                        | 否（顶洞不换组合）                  |
| **肉的分值类型**                  | 肉不含奖励 / 肉包含奖励                                                                | 肉不含奖励                      |
| **包洞触发条件**（四选一，默认“不包洞”）     | 1. 不包洞 2. 从 +N 开始包洞（N默认4） 3. 从 DOUBLEPAR + N 开始包洞（N默认0） 4. 与同伴杆差 N 杆包洞（N默认3） | 不包洞                        |
| **包洞前置条件**（仅当触发条件非“不包洞”时可选） | 1. 同伴顶头才包洞（同伴杆数 ≤ 对方最好成绩） 2. 与同伴成绩无关                                         | 与同伴成绩无关                    |

**游戏实例配置项**（创建具体游戏时填写）

| 配置项                  | 说明                                                                                                                                                                          | 默认值     |
|:-------------------- |:--------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |:------- |
| 游戏名称                 | 自定义标识                                                                                                                                                                       | “斗小地主”  |
| **积分系数 K**           | 每点对应积分                                                                                                                                                                      | 1       |
| **选择球员**             | 从本组球员列表中勾选三人                                                                                                                                                                | —       |
| **有效洞范围**            | 基于全程洞序的起始/结束索引                                                                                                                                                              | 全洞      |
| **分组方式**             | 固斗 / 乱斗                                                                                                                                                                     | 固斗      |
| **初始排序方式**           | 随机 / 手工指定 / 按差点排序（低到高）。该排序始终显示在配置界面，用户可随时调整，不依赖分组方式                                                                                                                         | 随机      |
| **排序规则**（分组方式为乱斗时显示） | 当两人调整后杆数相同时，按以下规则决定顺序： • **按出身成绩排序**：比较上一洞的调整后杆数，杆数低的排前；若仍相同，继续向前追溯，直到分出先后。 • **按输赢排序**：首先比较**本洞个人积分**（赢为正，输为负，平为0），积分高的排前；若本洞积分相同，则比较上一洞的个人积分（赢为正，输为负），积分高的排前；若仍相同，继续向前追溯。 | 按出身成绩排序 |
| **个人让杆值**            | 为每位球员分别设置按PAR3/4/5的让杆值（支持0.5倍数，可负）                                                                                                                                          | 0       |

#### 各分组方式详解

#### 1. 固斗

- 初始排序（通过初始排序方式确定）仅用于第一洞前的顺序，之后**固定不变**。
- 每洞均以**排序中的第三名**为单人队（A），**第一名和第二名**为双人队（B、C）。
- 后续洞不再重新排序（不受顶洞选项影响）。

#### 2. 乱斗

- 初始排序用于第一洞前的顺序。
- 每洞结束后，根据规则模板中的“顶洞时是否重新排序”决定：
  - 若选项为**“否”**（顶洞不换组合）：当本洞为顶洞时，排序保持不变；非顶洞时，根据本洞调整后杆数从低到高重新排序（杆数低者排前，相同则按“排序规则”处理）。
  - 若选项为**“是”**（顶洞也按成绩调整组合）：无论是否顶洞，每洞结束后都根据本洞调整后杆数重新排序（顶洞时也按杆数排序，相同按“排序规则”处理）。
- 下一洞按新排序，仍以第三名为单人队（A），第一、二名为双人队（B、C）。

#### 包洞规则详解（仅适用于双人队，有前置条件）

- **触发条件**：根据规则模板中选定的条件，判断双人队中哪位球员（第一名或第二名）的成绩达到了包洞标准。
- **前置条件**：
  - “同伴顶头才包洞”：只有当同伴的杆数 ≤ 对方最好成绩时，才执行包洞。
  - “与同伴成绩无关”：只要触发条件满足，就执行包洞。
- **执行规则**：
  - 若双人队中仅一人触发包洞，则该球员承担本队该洞的**所有正常负分**（即队友得0分），肉的损失各自承担（不转嫁）。
  - 若两人均触发包洞，则恢复正常平分（每人承担一半负分）。

**注意**：

- 个人让杆值按PAR3/4/5配置，每洞根据标准杆选择对应的让杆值。
- 有效洞范围可选。
- 无“高手不见面”分组方式。

#### 4.2.4 三人8421

#### 玩法说明

三人8421是三人游戏，每洞每位球员根据**在游戏实例中配置的成绩-得分映射表**获得个人得分。根据**排序（固定或动态）**，将**第一名和第三名**组成**双人队**，**第二名**为**单人队**。每洞比较 **（第一名得分 + 第三名得分）** 与 **第二名得分 × 2**，数值大者胜。设分差 D = | (第一名+第三名) - (第二名×2) |。

- **积分系数 K**（每点对应积分，游戏实例中配置）
- **积分分配**：
  - 若双人队（1+3）胜：两人各得 D·K，单人队（第2名）得 -2D·K。
  - 若单人队胜：单人队得 2D·K，双人队两人各得 -D·K。
  - 打平：所有球员 0 分。
- **支持机制**：扣分（仅当映射表中无定义时生效）、顶洞、吃肉（成绩-吃肉数量对应表或连续翻倍）、包负分（仅双人队）。
- **无让杆**：高差点通过自定义得分映射表调整。
- **顶洞定义**（规则模板）：得分打平 / 得分 ≤ 1 / 得分 ≤ 2 / 无顶洞。默认得分打平。
- **顶洞是否更换组合**（规则面板，仅乱斗）：**仅当顶洞定义为得分 ≤ 1 或得分 ≤ 2 时显示**。选项为「不换组合 / 更换组合」，默认**不换组合**。得分打平、无顶洞时**隐藏**该选项，不提供用户开关。固斗不受此项影响。
- **得分打平与排序**：打平条件为 `score1 + score3 = score2 × 2`，第2名的 score 是第1、第3名的算术平均。乱斗下 PUSH 洞仍走**正常 rerank**，不设置 freeze。组合通常自然保持，不是独立冻结规则。红蓝三角按当洞排序结果渲染。

#### 规则模板参数（保存在“我的规则”中）

| 参数             | 说明                                                                        | 默认值                 |
|:-------------- |:------------------------------------------------------------------------- |:------------------- |
| 扣分触发条件         | 从+N开始 / 从DOUBLEPAR+N开始 / 不扣分                                              | 从+4开始扣分，若选2，则N默认值为2 |
| 扣分上限           | 不封顶 / 扣N分封顶                                                               | 不封顶                 |
| 顶洞定义           | 得分打平 / 得分 ≤ 1 / 得分 ≤ 2 / 无顶洞                                               | 得分打平                |
| **顶洞是否更换组合**  | 不换组合 / 更换组合。**仅当顶洞定义 = 得分 ≤ 1 或得分 ≤ 2 时在规则面板显示**；得分打平、无顶洞时隐藏。只影响 ≤1 / ≤2 顶洞发生时的下一洞排序，不影响普通非顶洞、得分打平顶洞、固斗。 | 不换组合（仅 ≤1 / ≤2 时出现） |
| 吃肉数量对应表        | 成绩类别 → 吃肉数量（0~3或“全部”）                                                     | 所有成绩均对应1块肉          |
| 肉的分值类型         | 肉算N分 / 分值翻倍 / 一次全吃连续翻倍                                                    | 肉算N分（N=2）           |
| 肉值封顶           | 不封顶 / N分封顶                                                                | 不封顶                 |
| **包负分选项**（三选一） | 1. 不包负分 2. 同伴顶头才包负分（当双人队中球员触发扣分时，若同伴得分 ≤ 对方最好得分，则执行包负分） 3. 与同伴成绩无关（无条件执行） | 不包负分                |

**优先级说明**：成绩-得分映射表的得分项配置优先于扣分规则。对于任何 diff，首先查询映射表；仅当映射表中改项无得分（为0）的定义时，才进入扣分规则。

#### 游戏实例配置项（创建具体游戏时填写）

| 配置项                  | 说明                                                                                                                                                          | 默认值      |
|:-------------------- |:----------------------------------------------------------------------------------------------------------------------------------------------------------- |:-------- |
| 游戏名称                 | 自定义标识                                                                                                                                                       | “三人8421” |
| **积分系数 K**           | 得分映射表中1分对应积分                                                                                                                                                | 1        |
| **成绩-得分映射表**         | 用户可自定义，需提供 diff 范围对应的得分                                                                                                                                     | 默认表（见附注） |
| **选择球员**             | 从本组球员列表中勾选三人                                                                                                                                                | —        |
| **有效洞范围**            | 基于全程洞序的起始/结束索引                                                                                                                                              | 全洞       |
| **分组方式**             | 固斗 / 乱斗                                                                                                                                                     | 乱斗       |
| **初始排序方式**           | 随机 / 手工指定 / 按差点排序（低到高）。该排序始终显示在配置界面，用户可随时调整，不依赖分组方式                                                                                                         | 随机       |
| **排序规则**（分组方式为乱斗时显示） | 当两人**调整后得分**相同时，按以下规则决定顺序： • **按出身成绩排序**：比较上一洞的调整后得分，得分高的排前；若仍相同，继续向前追溯。 • **按输赢排序**：首先比较**本洞个人积分**（赢为正，输为负，平为0），积分高的排前；若相同，则比较上一洞的个人积分，积分高的排前；若仍相同，继续向前追溯。 | 按出身成绩排序  |

#### 分组方式详解

#### 1. 固斗

- 初始排序（通过初始排序方式确定）仅用于第一洞前的顺序，之后**固定不变**。
- 每洞均以**排序中的第一名和第三名**为双人队，**第二名**为单人队。
- 后续洞不再重新排序（顶洞不影响）。

#### 2. 乱斗

- 初始排序用于第一洞前的顺序。
- **所有非顶洞**：每洞结束后始终按本洞调整后得分（`rec.score`）从高到低正常重新排序；得分相同按「排序规则」处理。下一洞第1名和第3名为双人队，第2名为单人队。
- **顶洞 = 得分打平**：本洞结算、顶洞/肉状态照常。PUSH 洞仍走正常 rerank，不设置 freeze，规则面板不出现「顶洞是否更换组合」。因打平式使第2名为两端算术平均，单人/双人组合通常自然保持。
- **顶洞 = 得分 ≤ 1 或得分 ≤ 2**：本洞结算、顶洞/肉状态照常。此时 D 可以非 0，组合**不是**数学上天然保持，由面板选项决定下一洞身份：
  - **不换组合**（默认）：不使用本洞结果重新分组。下一洞继续沿用**本洞开始时**的组合身份：第2名仍为单人队，第1名和第3名仍为双人队。
  - **更换组合**：即使本洞属于 ≤1 / ≤2 顶洞，仍按本洞 `rec.score` / 调整后得分从高到低正常 rerank；相同按「排序规则」。下一洞第1+第3名为双人队，第2名为单人队。
- **无顶洞**：不产生顶洞；每洞结束后均正常 rerank。
- **实现原则（UI）**：红蓝三角不单独维护特殊逻辑，始终按**下一洞最终生效的排序/组合**渲染：不换组合 → 使用保留下来的 order；更换组合 → 使用 rerank 后的 order。

#### 包负分规则详解（仅适用于双人队，仅当一人触发扣分时生效）

- **触发条件**：双人队中**有且只有一人**的个人得分因扣分变为负值（即得分 < 0）。当双人队两人均发生扣分，则按照团队负分作为个人损益。
- **选项**：
  - **不包负分**：正常平分负分。
  - **同伴顶头才包负分**：当触发扣分时，若同伴的得分 ≤ 对方最好得分，则执行包负分；否则不执行。
  - **与同伴成绩无关**：无条件执行包负分。
- **执行算法**：
  - 设双人队基础积分（正常分配结果）为 `base = -D·K`（两人各得 base）。
  - 设触发扣分的球员个人得分为 `neg`（负值），其绝对值为 `p = -neg`。
  - 包负分后，该球员得分调整为 `base - p`，队友得分调整为 `base + p`。
  - 若两人均触发扣分，则正常平分（不执行包负分）。

**示例**：

- K=1，双人队（A、C），基础积分 `base = -5`（每人-5）。
- A 触发扣分，个人得分为 -1（p=1）。
- 执行包负分后：A = -5 - 1 = -6，C = -5 + 1 = -4。

#### 算法示例（完整洞计算）

- 实例配置：K=1，分组方式固斗，初始排序 [A, B, C]（A第一，B第二，C第三）。默认得分映射表。
- **第1洞**（par4）：A=3, B=4, C=5。
  - 得分：A=8（小鸟），B=4（PAR），C=2（柏忌）。
  - 双人队得分 = 8+2=10，单人队B×2=8 → 双人队胜，D=2。
  - 积分：A=+2，C=+2，B=-4。
- **第2洞**（par4）：A=8（diff=+4，映射表中无此diff，进入扣分，得分-1），B=4（4），C=4（4）。
  - 双人队得分 = -1+4=3，单人队4×2=8 → 单人队胜，D=5。
  - 基础分配：B=+10，A=-5，C=-5。
  - 若启用包负分（“与同伴成绩无关”），A触发扣分（得分-1），p=1。调整后：A=-5-1=-6，C=-5+1=-4，B不变=+10。
- **顶洞**：若打平，则本洞积分为0，累积1块肉。
- **吃肉**：非顶洞时，胜队根据其**个人最好成绩**（对双人队取两人中得分较高者？实际上吃肉数量对应表基于**成绩类别**，即根据胜队中用于比较的那个成绩对应的成绩类别来查表。三人8421的吃肉规则与四人8421相同，此处略。

#### 附注：默认成绩-得分映射表（示例）

| diff（实际杆数-标准杆） | 得分      |
|:-------------- |:------- |
| ≤ -3           | 32      |
| -2             | 16      |
| -1             | 8       |
| 0              | 4       |
| +1             | 2       |
| +2             | 1       |
| +3             | 0       |
| ≥ +4           | 由扣分设置决定 |

用户可自定义该表，且当自定义值为正数时优先于扣分规则。

### 4.3 四人游戏模板

#### 4.3.1 四人拉丝

#### 玩法说明

四人拉丝是一种队际赛，四人分为两队（每队两人），每洞比较两队选定的指标（较好成绩、较差成绩、总成绩），每个指标可设置权重，根据比较结果（胜+1，负-1，平0）乘以权重求和得到本洞**团队得分**，两队得分互为相反数。

- **个人基础积分** = 团队得分（即默认情况下队内两人得分相同）。
- **包洞时**，个人积分会重新分配（触发包洞的球员承担全部正常负分，队友得0，肉的损失各自承担），但调整后同队两人积分之和仍等于团队得分。
- 支持加法奖励、乘法奖励、顶洞、吃肉、包洞等机制。

#### 规则模板参数（保存在“我的规则”中）

| 参数            | 说明                                                                                                                                                                                                                                                  | 默认值                                                  |
|:------------- |:--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |:---------------------------------------------------- |
| 比较指标          | 较好成绩、较差成绩、总成绩（可多选）                                                                                                                                                                                                                                  | 三项全选                                                 |
| 指标权重          | 每个选中指标的正整数权重                                                                                                                                                                                                                                        | 均为1                                                  |
| 总成绩计算方式       | 和 / 积                                                                                                                                                                                                                                               | 和                                                    |
| 奖励模式          | 无 / 加法 / 乘法                                                                                                                                                                                                                                         | 无                                                    |
| **加法奖励配置**    | - 触发成绩及奖励分值：小鸟+1，老鹰+3，信天翁/HIO+10，PAR+0（可自定义）。 - 奖励分值需乘以触发指标对应的权重。 - **前置条件**（仅当用户选择的比较指标中包含“总成绩”时可选）：  • **总成绩赢才奖**（默认）：仅当团队总成绩获胜时，**胜队**获得奖励（若胜队中有球员触发奖励）。  • **总成绩不输才奖**：当团队总成绩不输（赢或平）时，**触发奖励的球员所在队伍**获得奖励。  • **与总成绩无关**：任何打出奖励成绩的球员，其所在队伍获得奖励。 | 小鸟+1，老鹰+3，信天翁/HIO+10，PAR+0；前置条件=总成绩赢才奖（当选择了总成绩指标的时候） |
| 乘法奖励配置        | 个人倍数、组合倍数，只有胜队的成绩才能触发乘法奖励，负队的成绩永远不会触发奖励倍数                                                                                                                                                                                                           | 小鸟×2，老鹰×5，信天翁/HIO×10；                                |
| 顶洞定义          | 得分打平 / 得分≤N / 无顶洞                                                                                                                                                                                                                                   | 得分打平                                                 |
| **顶洞时是否重新排序** | 是 / 否（即顶洞时是否根据成绩调整组合）                                                                                                                                                                                                                               | 否（顶洞不换组合）                                            |
| 吃肉数量对应表       | 成绩类别（比鸟更好、鸟、PAR、比PAR更差）→ 吃肉数量（0~3或“全部”）                                                                                                                                                                                                             | 比鸟更好=全部，鸟=2，PAR=1，比PAR更差=0                           |
| 肉的分值类型        | 分值翻倍（不含奖励）/ 分值翻倍（含奖励）/ 肉算N分                                                                                                                                                                                                                         | 分值翻倍（不含奖励）                                           |
| 肉值封顶          | 不封顶 / N分封顶（N默认3）                                                                                                                                                                                                                                    | 不封顶                                                  |
| 包洞触发条件        | 从+N开始 （N默认=4）/ 从DOUBLEPAR+N（N默认=0）开始 / 与同伴杆差N（N默认=3） / 不包洞                                                                                                                                                                                          | 不包洞                                                  |
| 包洞前置条件        | 同伴顶头才包洞 / 与同伴成绩无关                                                                                                                                                                                                                                   | 同伴顶头才包洞（当触发条件不为”不包洞时）                                |

#### 游戏实例配置项（创建具体游戏时填写）

| 配置项                     | 说明                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 默认值              |
|:----------------------- |:----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |:---------------- |
| 游戏名称                    | 自定义标识                                                                                                                                                                                                                                                                                                                                                                                                                                                             | “拉丝”             |
| **积分系数K**               | 每个指标比较结果1点对应积分（最终积分 = 团队得分 × 每点价值）                                                                                                                                                                                                                                                                                                                                                                                                                                | 1                |
| **选择球员**                | 从本组球员列表中勾选四人                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 四人全选且不可取消选取      |
| **有效洞范围**               | 基于全程洞序的起始/结束索引                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 全洞               |
| **编队方式**                | 乱拉 / 固拉 / 高手不见面                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 乱拉               |
| **初始排序方式**（乱拉或高手不见面时显示） | 随机 / 手工指定 / 按差点排序（低到高）                                                                                                                                                                                                                                                                                                                                                                                                                                            | 随机               |
| **排序规则**（乱拉或高手不见面时可选）   | 当无让杆配置时，只有前两个选项（后两项不可选），当有让杆配置时，四个选项均可单选。选择1/2中任意一个，默认按真实成绩排序。选择3/4默认按照调整后成绩排序。 1. **成绩相同按出身成绩排序**：本洞成绩相同的选手，比较上一洞的真实成绩，杆数低的排前；若相同，继续向前追溯。 2. **成绩相同按本洞输赢排序，输赢相同按出生输赢排序**：本洞成绩相同的选手，首先比较本洞的**个人积分**（赢为正，输为负，平为0），积分高的排前；若相同，则比较上一洞的个人积分，积分高的排前；若仍相同，继续向前追溯。 3. **受让后成绩相同按出生受让成绩排序**：本洞调整后成绩相同的选手，比较上一洞的**调整后杆数**（已应用让杆），杆数低的排前；若相同，继续向前追溯。 4. **受让后成绩相同按本洞输赢排序，输赢相同按出生输赢排序**：本洞调整后成绩相同的选手，首先比较本洞的**个人积分**（赢为正，输为负，平为0），积分高的排前；若相同，则比较上一洞的个人积分，积分高的排前；若仍相同，继续向前追溯。 | 选项1（成绩相同按出身成绩排序） |
| **个人让杆值**               | 为每位球员分别设置按PAR3/4/5的让杆值（支持0.5倍数，可负）                                                                                                                                                                                                                                                                                                                                                                                                                                | 0                |

#### 各分组方式详解

#### 1. 固拉

- 初始排序（通过初始排序方式确定）仅用于第一洞前的顺序，之后**固定不变**。
- 每洞均以**排序中的第一名和第四名**为A队，**第二名和第三名**为B队。
- 后续洞不再重新排序（顶洞不影响）。

#### 2. 乱拉

- 初始排序用于第一洞前的顺序。
- 每洞结束后，根据**排序规则**从低到高重新排序。
- 下一洞按新排序，仍以第一名和第四名为A队，第二名和第三名为B队。

#### 3. 高手不见面

- **适用前提**：四人中，前两名定义为“高手”，后两名定义为“低手”。
- 初始排序（通过初始排序方式确定）中的前两名为高手，后两名为低手。
- **第一洞**：以排序中的第一名（高手第1名）和第四名（低手第2名）为A队，第二名（高手第2名）和第三名（低手第1名）为B队。
- **每洞后的排序规则**：
  - 仅对前两名（高手）和后两名（低手）内部重新排序：高手组内按本洞调整后杆数从低到高排序，低手组内同样排序。杆数相同按“排序规则”处理。
  - 然后重新交叉组队：A队 = {高手第1名, 低手第2名}，B队 = {高手第2名, 低手第1名}。
  - 顶洞时排序保持不变（组内顺序不变，交叉组合不变）。
- **下一洞**按新组合进行。

#### 奖励、顶洞、吃肉、包洞详细说明

#### 加法奖励

- 奖励分值乘以触发指标对应的权重后，加到**触发奖励的球员所在队伍的团队得分**上。
- 前置条件决定了哪些情况下奖励生效（见规则模板参数）。

#### 乘法奖励

- 乘法奖励使**胜队和负队的团队得分**乘以倍数。
- 仅胜队的触发奖励成绩可触发乘法奖励，负队任何成绩不触发奖励。

#### 顶洞

- 顶洞定义：得分打平 / 得分≤N / 无顶洞。当得分<=N时，团队正常得分，但**累积1块肉**。

#### 吃肉

- 非顶洞时，胜队根据其**队内最好成绩**（两人中较低杆数）对应的成绩类别，按“成绩-吃肉数量对应表”决定吃肉数量（数字或“全部”）。
- 从累积肉池中取出相应数量的肉块，若不足则取全部。
- 每块肉价值按“肉的分值类型”计算（分值翻倍或肉算N分）。
- 肉的总价值加到胜队团队得分上，负队团队得分相应减少。

#### 包洞

- **触发对象**：仅对双人队中的球员有效。
- **触发条件**：根据规则模板中的包洞触发条件判断（如杆数超过标准杆+N等）。
- **前置条件**：同伴顶头才包洞 或 与同伴成绩无关。
- **执行规则**：
  - 若双人队中仅一人触发包洞，则**该球员承担本队该洞的所有正常负分**（即队友得0分，自己获得团队总负分）。肉的损失各自承担（不转嫁）。
  - 若两人均触发包洞，则恢复正常平分（每人承担一半）。
- **注意**：包洞只影响**正常负分**部分，肉的损失（吃肉产生的额外分数）仍由两人各自承担（不因包洞而转移）。

#### 算法示例（包含包洞）

**实例配置**：每点价值=1，编队方式固斗，初始排序 [A, B, C, D]。指标全选，权重均为1。总成绩计算方式=和。加法奖励（小鸟+1，前置条件“总成绩赢才奖”），顶洞定义=得分打平，吃肉数量对应表默认，肉分值翻倍（不含奖励）。包洞触发条件“从+4开始包洞”，前置条件“与同伴成绩无关”。

#### 第1洞（正常情况）

- 杆数：A=4, B=5, C=5, D=6。
- 分队：A队（A,D），B队（B,C）。
- 较好成绩：A=4 vs B=5 → A队胜+1
- 较差成绩：D=6 vs C=5 → B队胜-1（A队-1）
- 总成绩（和）：A+D=10 vs B+C=10 → 平0
- 团队得分（不含奖励）：A队=0，B队=0。每人积分0。

#### 第2洞（正常，有奖励）

- 杆数：A=3（小鸟），B=4，C=6，D=4。
- 较好：A=3 vs B=4 → A队胜+1
- 较差：D=4 vs C=6 → A队胜+1
- 总成绩：A+D=7 vs B+C=10 → A队胜+1
- 团队得分（不含奖励）：A队=3，B队=-3。
- 奖励：A打出小鸟，前置条件满足，A队团队得分+1 → A队=4，B队=-4。
- 每人积分：A=+4，D=+4，B=-4，C=-4。

#### 第3洞（包洞示例）

- 杆数：A=4, B=8（触发扣分），C=5, D=4。
- 分队：A队（A,D），B队（B,C）。
- 较好：A=4 vs B=8 → A队胜+1
- 较差：D=4 vs C=5 → A队胜+1
- 总成绩：A+D=8 vs B+C=13 → A队胜+1
- 团队得分（不含奖励）：A队=3，B队=-3。
- 包洞：B杆数8（par4，diff=+4），触发包洞条件，且前置条件“与同伴成绩无关”。B触发包洞，承担C的正常负分（-3），C得0。A队不变。
- 最终个人积分：A=+3，D=+3，B=-6，C=0。

#### 吃肉示例

- 累积肉块=1，第4洞A队胜，原始团队得分3（不含肉），胜队最好成绩为A（小鸟），查表吃肉数量=2，取1块。肉价值=原始团队得分×2=6。A队团队得分=3+6=9，B队=-9。每人积分：A=+9，D=+9，B=-9，C=-9。

#### 4.3.2 四人8421

#### 玩法说明

四人8421是一种个人得分后团队对抗的游戏。每洞每位球员根据**成绩-得分映射表**（仅可配置 diff = -3 到 +3）获得个人得分。四人根据**编队方式**（固拉、乱拉、高手不见面）分成两队，每队两人，比较两队总分，总分高者胜。两队总分差 D 乘以每分价值得到该洞的团队得分，胜队每人获得该得分，负队每人扣除相同得分。支持扣分、顶洞、吃肉（含连续翻倍）、包负分等机制。

- **无让杆**（高差点通过自定义得分映射表调整）。
- **团队得分** = D × 每分价值。
- **个人积分** = 团队得分（默认两人相同），包负分时可能重新分配。
- **顶洞时组合调整**：固定为**顶洞时不重新排序**（即顶洞时排序保持不变，组合不变）。

#### 规则模板参数（保存在“我的规则”中）

| 参数            | 说明                                         | 默认值                  |
|:------------- |:------------------------------------------ |:-------------------- |
| 扣分触发条件        | 从+N开始（N默认=4） / 从DOUBLEPAR+N（N默认=0）开始 / 不扣分 | 从+4开始扣分              |
| 扣分上限          | 不封顶 / 扣N（N默认=2）分封顶                         | 不封顶                  |
| 顶洞定义          | 得分打平 / 得分≤N （N默认=1）/ 无顶洞                   | 得分打平                 |
| **顶洞时是否重新排序** | 是 / 否（即顶洞时是否根据成绩调整组合）                      | 否（顶洞不换组合）            |
| 吃肉数量对应表       | 成绩类别（比鸟更好、鸟、PAR、比PAR更差）→ 吃肉数量（0~3或“全部”）    | 全部类别默认 **1**（用户可自定义） |
| 肉的分值类型        | 肉算N分 / 分值翻倍 / 一次全吃连续翻倍                     | 肉算N分（N=2）            |
| 肉值封顶          | 不封顶 / N（N默认=3）分封顶                          | 不封顶                  |
| 包负分选项         | 1. 不包负分 2. 同伴顶头才包负分 3. 与同伴成绩无关             | 不包负分                 |

#### 成绩-得分映射表配置说明

- **可配置范围**：`diff = -3, -2, -1, 0, +1, +2, +3` 七个档位。

- **配置方式**：每个档位使用滑轮选择，数值为 **非负整数**（≥0）。

- **默认值**：

  | diff | 得分  |
  |:---- |:--- |
  | ≤ -3 | 50  |
  | -2   | 16  |
  | -1   | 8   |
  | 0    | 4   |
  | +1   | 2   |
  | +2   | 1   |
  | +3   | 0   |

- **未定义区域（diff ≥ +4）**：用户无法自定义得分。系统根据**扣分规则**处理：

  - 若扣分规则启用（例如“从 +N 开始”），则按扣分规则计算得分（可为负或零）。
  - 若扣分规则设为“不扣分”，则得分为 0。

**优先级说明**：

- 系统首先检查 diff 是否在 -3 到 +3 范围内：
  - 若是且映射得分 > 0，则使用映射得分。
  - 若是但映射得分 = 0，则进入扣分规则（若启用）。
  - 若否（diff ≥ +4），则进入扣分规则（若启用），否则得 0。

#### 游戏实例配置项（创建具体游戏时填写）

| 配置项                     | 说明                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 默认值         |
|:----------------------- |:---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |:----------- |
| 游戏名称                    | 自定义标识                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | “四人8421”    |
| **积分系数K**               | 得分映射表中1分对应积分（最终积分 = D × 每分价值）                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 1           |
| **成绩-得分映射表**            | 用户自定义 diff = -3..+3 的得分（非负整数）                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 默认表（见上）     |
| **选择球员**                | 从本组球员列表中勾选四人                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 默认四人全选且不可取消 |
| **有效洞范围**               | 基于全程洞序的起始/结束索引                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 全洞          |
| **编队方式**                | 固拉 / 乱拉 / 高手不见面                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 固拉          |
| **初始排序方式**（乱拉或高手不见面时显示） | 随机 / 手工指定 / 按差点排序（低到高）                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 随机          |
| **排序规则**（乱拉或高手不见面时显示）   | **可用性**：当所有球员的得分映射表完全相同时，只有选项1、2可选；否则四个选项全可选。 **选项定义**： 1. **成绩相同按出身成绩排序**：第一排序依据为本洞**真实杆数**（低者排前），若杆数相同，则比较上一洞的真实杆数，杆数低的排前；若仍相同，继续向前追溯。 2. **成绩相同按本洞输赢排序，输赢相同按出生输赢排序**：第一排序依据为本洞**真实杆数**，若杆数相同，则比较本洞**个人积分**（赢为正，输为负，平为0），积分高的排前；若积分相同，则比较上一洞的个人积分，积分高的排前；若仍相同，继续向前追溯。 3. **得分相同按出生得分排序**：第一排序依据为本洞**得分**（得分高者排前），若得分相同，则比较上一洞的得分，得分高的排前；若仍相同，继续向前追溯。 4. **得分相同按本洞输赢排序，输赢相同按出生输赢排序**：第一排序依据为本洞**得分**，若得分相同，则比较本洞**个人积分**（赢为正，输为负，平为0），积分高的排前；若积分相同，则比较上一洞的个人积分，积分高的排前；若仍相同，继续向前追溯。 **默认**：选项1 | 选项1         |

#### 各分组方式详解

#### 1. 固拉

- 初始排序（通过初始排序方式确定）仅用于第一洞前的顺序，之后**固定不变**。
- 每洞均以**排序中的第一名和第四名**为A队，**第二名和第三名**为B队。
- 后续洞不再重新排序（顶洞不影响）。

#### 2. 乱拉

- 初始排序用于第一洞前的顺序。
- 每洞结束后，**根据用户选择的排序规则决定排序依据**：
  - 若选择选项1或2，则根据本洞**真实杆数**从低到高排序（杆数低者排前）；若杆数相同，再按规则中的具体逻辑（出身真实成绩或输赢）决定顺序。
  - 若选择选项3或4，则根据本洞**得分**从高到低排序（得分高者排前）；若得分相同，再按规则中的具体逻辑（出生得分或输赢）决定顺序。
- **注意**：顶洞时排序保持不变（不重新排序）。
- 下一洞按新排序，仍以第一名和第四名为A队，第二名和第三名为B队。

#### 3. 高手不见面

- **适用前提**：四人中，前两名定义为“高手”，后两名定义为“低手”。
- 初始排序（通过初始排序方式确定）中的前两名为高手，后两名为低手。
- **第一洞**：以排序中的第一名和第四名为A队，第二名和第三名为B队。
- **每洞后的排序规则**：
  - 仅对前两名（高手）和后两名（低手）内部重新排序。排序依据同样取决于用户选择的选项（1/2 按真实杆数，3/4 按得分），相同再按规则处理。
  - 然后重新交叉组队：A队 = {高手第1名, 低手第2名}，B队 = {高手第2名, 低手第1名}。
- **下一洞**按新组合进行。

#### 顶洞、吃肉、包负分详细说明

#### 扣分与得分映射

- 每洞根据实际杆数与标准杆的差值（diff）先查映射表（仅 -3..+3），若在范围内且得分>0则用映射得分，否则进入扣分规则。
- 扣分规则：若 diff ≥ N（或满足 DOUBLEPAR+N 条件），每多一杆扣1分，受扣分上限限制。

#### 顶洞

- 顶洞定义：得分=0/得分<=N，
- 顶洞时：本洞团队得分正常，但**累积1块肉**。

#### 吃肉

- 非顶洞时，胜队根据其**队内个人最好成绩**（个人得分最高者对应的成绩类别）查“成绩-吃肉数量对应表”决定吃肉数量。
- 从累积肉池中取出相应数量的肉块，若不足则取全部。
- 每块肉价值按“肉的分值类型”计算：
  - **肉算N分**：固定 N 分（默认2）。
  - **分值翻倍**：每块肉价值 = 本洞胜队每人所得积分（即 D × 每分价值）乘以2。
  - **一次全吃连续翻倍**：选择此项时，第一部分（吃肉数量）变灰，不再根据成绩表吃肉。初始倍数=1，每顶洞一次下一洞倍数翻倍；有胜队时，胜队得分 × 当前倍数，倍数重置。
- 肉的总价值加到胜队团队得分上，负队团队得分相应减少，从而影响每人积分。

#### 包负分

- **触发条件**：双人队有且只有一名球员的个人得分因扣分变为负值（即得分<0），若两人均为负值，则不触发包负分。
- **选项**：
  - **不包负分**：正常平分负分。
  - **同伴顶头才包负分**：当球员触发扣分时，仅当同伴的得分 >=对方最好得分，才执行包负分。
  - **与同伴成绩无关**：无条件执行包负分。
- **执行规则**（仅当一人触发时）：
  - 设双人队基础积分（正常分配结果）为 `base = -D × 每分价值`（每人 base）。
  - 设触发扣分的球员个人得分为 `neg`（负值），其绝对值为 `p = -neg`。
  - 包负分后，该球员得分调整为 `base - p`，队友得分调整为 `base + p`。
  - 若两人均触发，则正常平分（不包）。

#### 算法示例

**实例配置**：每分价值=1，编队方式固拉，初始排序 [A, B, C, D]。默认得分映射表，扣分从+4开始，吃肉数量对应表全1，肉分值类型=肉算N分（N=2），包负分选项=与同伴成绩无关。

#### 第1洞（正常）

- 杆数：A=3, B=4, C=5, D=6。
- 得分：A=8（小鸟），B=4（PAR），C=2（柏忌），D=1（+2杆）。
- 分队：A队（A,D）=8+1=9，B队（B,C）=4+2=6，A队胜，D=3。
- 基础团队得分：A队+3，B队-3。
- 每人积分：A=+3，D=+3，B=-3，C=-3。
- 无吃肉、无顶洞。

#### 第2洞（扣分触发）

- 杆数：A=8, B=4, C=4, D=4。
- 得分：A diff=+4，不在映射表范围，进入扣分规则（从+4开始）：扣1分，得-1；B=4，C=4，D=4。
- 分队：A队（A,D）=-1+4=3，B队（B,C）=4+4=8，B队胜，D=5。
- 基础团队得分：B队+5，A队-5。
- 每人积分：B=+5，C=+5，A=-5，D=-5。
- 包负分：A触发扣分（得分-1），且包负分选项为“与同伴成绩无关”，执行包负分。A额外承担 p=1，A = -5-1 = -6，D = -5+1 = -4。最终积分：A=-6，D=-4，B=+5，C=+5。

#### 顶洞示例

- 两队总分相等，团队得分0，累积1块肉。

#### 吃肉示例

- 累积肉块=1，下一洞A队胜，D=3，基础团队得分A队+3，B队-3。吃肉：胜队最好成绩为A（小鸟，类别鸟），查表吃肉数量=1，吃1块肉。肉价值=肉算N分=2。A队团队得分=3+2=5，B队=-5。每人积分：A=+5，D=+5，B=-5，C=-5。

#### 连续翻倍示例

#### 实例配置

- 每分价值 = 1
- 编队方式：固拉，初始排序 [A, B, C, D]（A第一，B第二，C第三，D第四）
- 成绩-得分映射表：默认（小鸟8，PAR4，柏忌2，+2杆1分，+4扣分-1）
- 扣分规则：从+4开始扣分（不启用扣分上限）
- **肉的分值类型**：一次全吃连续翻倍（不吃肉数量表，不消耗肉块，根据顶洞次数累积倍数）
- 顶洞定义：得分打平
- 其他配置：无奖励、无包负分

#### 过程

#### 第1洞（par4）

- 杆数：A=4, B=4, C=4, D=4 → 全部PAR，得分均为4。
- 分队：A队（A,D）=4+4=8，B队（B,C）=4+4=8 → 总分相等，**顶洞**。
- 团队得分0，每人积分0。
- 顶洞次数+1，当前倍数 = 1（初始为1，但顶洞后下一洞倍数翻倍？规则：每顶洞一次，下一洞倍数翻倍。因此第1洞顶洞后，第2洞倍数 = 1×2 = 2。）

#### 第2洞（par4）

- 杆数：A=4, B=4, C=4, D=4 → 全部PAR，得分均为4。
- 分队：A队（A,D）=8，B队（B,C）=8 → 再**顶洞**。
- 团队得分0，每人积分0。
- 顶洞次数+1，当前倍数 = 2×2 = 4。

#### 第3洞（par4）

- 杆数：A=4, B=4, C=4, D=4 → 全部PAR，**顶洞**。
- 团队得分0，每人积分0。
- 顶洞次数+1，当前倍数 = 4×2 = 8。

#### 第4洞（par4）

- 杆数：A=4, B=4, C=4, D=4 → 全部PAR，**顶洞**。
- 团队得分0，每人积分0。
- 顶洞次数+1，当前倍数 = 8×2 = 16。

#### 第5洞（par4）

- 杆数：A=4, B=4, C=4, D=6（+2杆，得分1）。
- 得分：A=4, B=4, C=4, D=1。
- 分队：A队（A,D）=4+1=5，B队（B,C）=4+4=8 → A队胜，分差 D = |5-8| = 3。
- 当前倍数 = 16，**非顶洞**，应用倍数：团队得分 = D × 每分价值 × 倍数 = 3 × 1 × 16 = 48。
- 胜队（A队）每人得 +48，负队（B队）每人得 -48。
- 倍数重置为1（下一洞重新累积）。

#### 结果汇总

| 洞号  | 顶洞？ | 倍数（应用前） | 胜队  | 分差  | 应用倍数后团队得分 | A   | B   | C   | D   |
|:--- |:--- |:------- |:--- |:--- |:--------- |:--- |:--- |:--- |:--- |
| 1   | 是   | 1 → 2   | —   | —   | 0         | 0   | 0   | 0   | 0   |
| 2   | 是   | 2 → 4   | —   | —   | 0         | 0   | 0   | 0   | 0   |
| 3   | 是   | 4 → 8   | —   | —   | 0         | 0   | 0   | 0   | 0   |
| 4   | 是   | 8 → 16  | —   | —   | 0         | 0   | 0   | 0   | 0   |
| 5   | 否   | 16 → 1  | A队  | 3   | 48        | +48 | -48 | -48 | +48 |

> 注意：第5洞A队成员为A和D，所以A和D各+48；B队成员为B和C，各-48。

#### 4.3.3 固定三打一（最终完整版，修正肉价值）

#### 玩法说明

固定三打一是一种四人游戏，四人固定分为两队：**单人队（A）** 与 **三人队（B、C、D）**。每洞，用户选择一种比较方式（最好成绩、平均成绩、最差成绩），然后比较两队成绩，杆数低者胜。积分分配（零和）：

- 单人队基础单位 = 3 点，三人队基础单位 = 1 点。
- 设积分系数 K（每点对应积分，游戏实例中配置，默认1）。
- **A 胜**：A +3K，B、C、D 各 -K。
- **三人队胜**：A -3K，B、C、D 各 +K。
- **打平**：所有球员 0 分。
- 支持乘法奖励（可选）、顶洞、吃肉，**无包洞**。

#### 规则模板参数（保存在“我的规则”中）

| 参数      | 说明                                 | 默认值   |
|:------- |:---------------------------------- |:----- |
| 比较方式    | 最好成绩 / 平均成绩 / 最差成绩                 | 最好成绩  |
| 奖励模式    | 无 / 乘法                             | 无     |
| 乘法奖励配置表 | 成绩对应倍数（小鸟×2，老鹰×5，信天翁/HIO×10，PAR×1） | 同左    |
| 顶洞定义    | 得分打平 / 无顶洞                         | 得分打平  |
| 肉的分值类型  | 肉不含奖励 / 肉包含奖励                      | 肉不含奖励 |
| 吃肉数量    | 每次获胜吃的肉块数（0~3或“全部”），默认1            | 1     |

#### 游戏实例配置项（创建具体游戏时填写）

| 配置项        | 说明                                     | 默认值     |
|:---------- |:-------------------------------------- |:------- |
| 游戏名称       | 自定义标识                                  | “固定三打一” |
| **积分系数 K** | 每点对应积分                                 | 1       |
| **选择球员**   | 从本组球员列表中勾选四人，并指定其中一人为 A（单人队），其余为 B、C、D | —       |
| **有效洞范围**  | 基于全程洞序的起始/结束索引                         | 全洞      |
| **个人让杆值**  | 为每位球员分别设置按PAR3/4/5的让杆值（支持0.5倍数，可负）     | 0       |

> 注：固定三打一没有编队方式的动态变化（因为分组固定），因此不需要初始排序、排序规则等配置。

#### 每洞计算流程

1. 根据个人让杆值计算每人的调整后杆数（实际杆数 - 让杆值）。
2. 根据用户选择的比较方式计算两队成绩：
   - **最好成绩**：单人队成绩 = A的调整后杆数；三人队成绩 = min(B, C, D的调整后杆数)。
   - **平均成绩**：单人队成绩 = A的调整后杆数；三人队成绩 = (B+C+D)/3（保留一位小数）。
   - **最差成绩**：单人队成绩 = A的调整后杆数；三人队成绩 = max(B, C, D的调整后杆数)。
3. 比较：杆数低者胜。若相等则视为顶洞。
4. **基础积分分配**（无奖励、无吃肉）：
   - 若 A 胜：A = +3K，B、C、D = -K。
   - 若三人队胜：A = -3K，B、C、D = +K。
   - 打平：所有人 = 0。
5. **奖励**（若启用乘法奖励）：
   - 触发条件：胜队用于比较的那个成绩（即胜队成绩）达到小鸟、老鹰等。
   - 倍数 M 从配置表获取。
   - 胜队每人基础积分乘以 M，负队每人相应扣除（保持零和）。例如 A 胜且小鸟×2：A = +6K，B、C、D = -2K。
6. **顶洞与吃肉**：
   - 顶洞定义：两队成绩相等。
   - 顶洞时：本洞积分为0，**累积1块肉**（跨洞累积）。下一洞排序不变（无排序，但影响吃肉）。
   - 非顶洞且累积肉块数 > 0时：
     - 胜队根据**吃肉数量**（规则模板中配置，默认1）吃掉相应数量的肉块（若数字N大于累积肉块数，则只吃掉所有）。
     - **每块肉的基础价值**（不含奖励）固定为 **3 点**。分配比例：
       - 若 A 胜：A 获得 +3 点，B、C、D 各获得 -1 点。
       - 若三人队胜：每人获得 +1 点，A 获得 -3 点。
     - **肉的分值类型**：
       - **肉不含奖励**：每块肉价值 = 3 点。
       - **肉包含奖励**：每块肉价值 = 本洞胜队所获得的总点数（已含奖励）。胜队总点数 = |胜队每人基础积分之和| / K（因为基础积分含K，点数=积分/K）。实际上，胜队总点数 = 3 × M（若胜队有奖励倍数 M），否则为 3。分配比例同上（A得3M，三人各得M）。
     - 实际积分 = 点数 × K。
     - 消耗的肉块数从累积池中减去。

#### 算法示例

**实例配置**：K=1，比较方式=最好成绩，奖励模式=无，顶洞定义=得分打平，肉不含奖励，吃肉数量=1。个人让杆值全0。

#### 第1洞（正常，无吃肉）

- 杆数：A=4, B=5, C=5, D=6。
- 三人队最好成绩 = min(5,5,6)=5，A=4 → A胜。
- 基础积分：A=+3，B、C、D各-1。
- 无奖励，无肉。

#### 第2洞（有吃肉）

- 假设第1洞后累积肉块=1（由之前顶洞产生）。
- 杆数：A=4, B=3, C=5, D=6。
- 三人队最好成绩 = min(3,5,6)=3，A=4 → 三人队胜。
- 基础积分：A=-3，B=+1，C=+1，D=+1。
- 非顶洞，累积肉=1，吃肉数量=1，胜队（三人队）吃肉。肉不含奖励，每块肉基础价值3点，分配：三人队每人 +1 点，A -3 点。
- 最终积分：A = -3-3 = -6，B = 1+1 = 2，C = 1+1 = 2，D = 1+1 = 2。
- 累积肉块清零。

#### 顶洞示例

- 杆数：A=4, B=4, C=4, D=4。
- 三人队最好成绩=4，A=4 → 打平，顶洞。
- 本洞积分0，累积肉块+1。

#### 奖励 + 肉含奖励示例

- 配置：奖励模式=乘法（小鸟×2），肉含奖励，吃肉数量=1。
- 杆数：A=3（小鸟）, B=4, C=4, D=4 → 三人队最好成绩=4，A=3 → A胜。
- 基础积分（含奖励）：A = 3×2=+6，B、C、D = -2。
- 胜队总点数 = 6（因为K=1，点数=积分），肉含奖励，每块肉价值 = 胜队总点数 = 6 点。分配：A 得 6 点，B、C、D 各得 -2 点。
- 最终积分：A = 6+6=+12，B、C、D = -2-2=-4 每人。

#### 4.3.4 四人地主婆

#### 玩法说明

四人地主婆是一种四人游戏，分为两队：**地主队**（2人）和**农民队**（2人）。其中一名固定球员为“地主婆”（D），其余三人（A、B、C）中，根据**排序**每洞选出一名“地主”或“二地主”与D组队。支持两种模式（在游戏实例中选择）：

- **四人大地主婆**：比较地主队的最好成绩 vs 农民队的最好成绩，杆数低者胜。
- **四人二地主婆**：比较地主队的最好成绩 vs 农民队的平均成绩，数值小者胜。

积分分配（零和）：

- 设积分系数 K（每点对应积分，游戏实例中配置，默认1）。
- 胜队每人 +K，负队每人 -K。
- 打平：所有球员 0 分。

支持乘法奖励、顶洞、吃肉（成绩-吃肉数量对应表，肉分值类型二选一），**无包洞**，**无让杆**（所有比较基于真实杆数）。**但排序规则中允许使用让杆后的成绩（选项3/4），这需要游戏实例中为球员配置让杆值。** 让杆属于游戏实例配置。

#### 规则模板参数（保存在“我的规则”中）

| 参数            | 说明                                      | 默认值                                |
|:------------- |:--------------------------------------- |:---------------------------------- |
| 奖励模式          | 无 / 乘法                                  | 无                                  |
| 乘法奖励配置表       | 成绩对应倍数（小鸟×2，老鹰×5，信天翁/HIO×10，PAR×1）      | 同左                                 |
| 顶洞定义          | 得分打平 / 无顶洞                              | 得分打平                               |
| **顶洞时是否重新排序** | 是 / 否（即顶洞时是否根据成绩调整组合）                   | 否                                  |
| 成绩-吃肉数量对应表    | 成绩类别（比鸟更好、鸟、PAR、比PAR更差）→ 吃肉数量（0~3或“全部”） | 比鸟更好=全部，鸟=2，PAR=1，比PAR更差=0（用户可自定义） |
| 肉的分值类型        | 肉不含奖励 / 肉包含奖励                           | 肉不含奖励                              |

#### 游戏实例配置项（创建具体游戏时填写）

| 配置项                     | 说明                                                                                                                                                                                                                                                                                                                                                                                                    | 默认值     |
|:----------------------- |:----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |:------- |
| 游戏名称                    | 自定义标识                                                                                                                                                                                                                                                                                                                                                                                                 | “四人地主婆” |
| **积分系数 K**              | 每点对应积分（胜队每人 +K，负队每人 -K）                                                                                                                                                                                                                                                                                                                                                                               | 1       |
| **模式**                  | 大地主婆 / 二地主婆                                                                                                                                                                                                                                                                                                                                                                                           | 大地主婆    |
| **选择球员**                | 从本组球员列表中勾选四人，并指定其中一人为 D（地主婆），其余为 A、B、C                                                                                                                                                                                                                                                                                                                                                                | —       |
| **有效洞范围**               | 基于全程洞序的起始/结束索引                                                                                                                                                                                                                                                                                                                                                                                        | 全洞      |
| **分组方式**                | 固拉 / 乱拉（决定每洞地主/二地主的选取方式）                                                                                                                                                                                                                                                                                                                                                                              | 固拉      |
| **初始排序方式**（分组方式为乱拉时显示）  | 随机 / 手工指定 / 按差点排序（低到高）                                                                                                                                                                                                                                                                                                                                                                                | 随机      |
| **排序规则**（分组方式为乱拉时显示）    | 当两人本洞杆数相同时，按以下规则决定顺序（与四人拉丝完全相同）。**可用性**：选项3/4仅在游戏实例中配置了让杆时可用；选项1/2始终可用。 1. **成绩相同按出身成绩排序**：比较上一洞的真实杆数，杆数低的排前；若相同，继续向前追溯。 2. **成绩相同按本洞输赢排序，输赢相同按出生输赢排序**：比较本洞的**个人积分**（赢为正，输为负，平为0），积分高的排前；若相同，则比较上一洞的个人积分，积分高的排前；若仍相同，继续向前追溯。 3. **受让后成绩相同按出生受让成绩排序**：比较上一洞的**调整后杆数**（已应用让杆），杆数低的排前；若相同，继续向前追溯。 4. **受让后成绩相同按本洞输赢排序，输赢相同按出生输赢排序**：比较本洞的**个人积分**，积分高的排前；若相同，则比较上一洞的个人积分，积分高的排前；若仍相同，继续向前追溯。 **默认**：选项1 | 选项1     |
| **个人让杆值**（供排序规则选项3/4使用） | 为每位球员分别设置按PAR3/4/5的让杆值（支持0.5倍数，可负）                                                                                                                                                                                                                                                                                                                                                                    | 0       |

#### 角色分配详解

#### 固拉模式

- 初始排序（通过初始排序方式确定）仅用于第一洞前的顺序，之后**固定不变**。
- 每洞均以排序中的**第一名**为大地主（大地主婆模式）或**第二名**为二地主（二地主婆模式），与D组成地主队；剩余两人为农民队。

#### 乱拉模式

- 初始排序用于第一洞前的顺序。
- 每洞结束后，根据规则模板中的“顶洞时是否重新排序”决定：
  - 若选项为**“否”**（顶洞不换组合）：当本洞为顶洞时，排序保持不变；非顶洞时，根据用户选择的排序规则重新排序（依据真实杆数或调整后杆数，取决于选项）。
  - 若选项为**“是”**（顶洞也按成绩调整组合）：无论是否顶洞，每洞结束后都按排序规则重新排序。
- 下一洞按新排序，仍以第1/2名作为大地主/二地主，与D组队。

#### 每洞计算流程

1. 根据模式确定本洞的地主/二地主（从A、B、C中按排序选取）。
2. 地主队 = {地主/二地主, D}，农民队 = 剩余两人。
3. 计算两队成绩：
   - **大地主婆**：地主队最好成绩 = min(地主/二地主杆数, D杆数)；农民队最好成绩 = min(农民两人杆数)。
   - **二地主婆**：地主队最好成绩 = min(地主/二地主杆数, D杆数)；农民队平均成绩 = (农民两人杆数和)/2（保留一位小数）。
4. 比较：杆数低者胜（大地主婆）或数值小者胜（二地主婆）。若相等则顶洞。
5. **基础积分分配**（无奖励、无吃肉）：
   - 胜队每人 +K，负队每人 -K。
   - 打平：所有人 0。
6. **奖励**（若启用乘法奖励）：
   - 触发条件：胜队用于比较的那个成绩（大地主婆：胜队最好成绩；二地主婆：胜队最好成绩或农民队平均成绩）达到小鸟、老鹰等。
   - 倍数 M 从配置表获取。
   - 胜队每人得分变为 +K × M，负队每人变为 -K × M。
7. **顶洞与吃肉**：
   - 顶洞时：本洞积分0，累积1块肉，下一洞排序根据“顶洞时是否重新排序”决定（见乱拉模式说明）。
   - 非顶洞且累积肉块数 > 0时：
     - 胜队根据其**队内最好成绩**（两人中较低杆数）对应的成绩类别，从累积池中取出相应数量的肉块（由成绩-吃肉数量对应表决定，0~3或“全部”）。
     - **每块肉价值**：
       - 肉不含奖励：每块肉价值固定为 **1 点**，实际积分 = 1 × K。
       - 肉含奖励：每块肉价值 = 本洞胜队每人已含奖励的原始得分（即 +K × M），实际积分 = 该值。
     - 胜队每人获得（肉块数 × 每块肉价值）积分，负队每人扣除相同积分。

#### 算法示例

**实例配置**：K=1，模式=大地主婆，固拉，初始排序 [A, B, C, D]（A第一，B第二，C第三，D为地主婆）。奖励模式=无，顶洞定义=得分打平，顶洞时是否重新排序=否，成绩-吃肉数量对应表默认，肉不含奖励。个人让杆值全0。

#### 第1洞（正常，无吃肉）

- 杆数：A=4, B=5, C=5, D=4。
- 地主：A，地主队（A,D）=min(4,4)=4；农民队（B,C）=min(5,5)=5 → 地主队胜。
- 基础积分：A=+1，D=+1，B=-1，C=-1。

#### 第2洞（有吃肉）

- 假设第1洞后累积肉块=1（由之前顶洞产生）。
- 杆数：A=4, B=3, C=5, D=4。
- 地主队最好成绩=4；农民队最好成绩=3 → 农民队胜。
- 基础积分：A=-1，D=-1，B=+1，C=+1。
- 非顶洞，累积肉=1，胜队（农民队）吃肉。肉不含奖励，每块肉价值1点（K=1，积分为1），胜队每人 +1，负队每人 -1。
- 最终积分：A = -1-1 = -2，D = -1-1 = -2，B = 1+1 = 2，C = 1+1 = 2。
- 累积肉块清零。

#### 顶洞示例（顶洞时不换组合）

- 所有杆数相同（如4,4,4,4）。
- 地主队最好成绩=4，农民队最好成绩=4 → 打平，顶洞。
- 本洞积分0，累积肉块+1。

#### 4.3.5 拉斯维加斯（Las Vegas）

#### 玩法说明

拉斯维加斯是一种四人游戏，四人分为两队（每队两人）。每洞，每名球员记录真实杆数（可应用让杆，仅整数），然后每队将两人的调整后杆数按照特定规则组合成一个数字。两队比较组合数字，数字小者胜，两队数字之差的绝对值即为该洞的团队得分，胜队每人获得该得分，负队每人扣除相同得分（乘以积分系数K）。支持翻牌（双向）、顶洞、吃肉、组合分数封顶等机制。

- **编队方式**：固拉、乱拉、高手不见面（在游戏实例中配置）。
- **让杆**：仅支持整数，按PAR3/4/5配置（可负）。每洞调整后杆数 = 实际杆数 - 让杆值。
- **组合成绩规则**：
  - 设两名球员的调整后杆数分别为 X 和 Y，且 X ≤ Y（排序后）。
  - **情况1**：Y ≤ 9（两人均为个位数） → 组合成绩 = X × 10 + Y（两位数）。例如 4和5 → 45。
  - **情况2**：X ≥ 10（两人均为两位数） → 组合成绩 = X × 10 + Y（三位数或四位数）。例如 10和12 → 10×10+12=112；11和14 → 11×10+14=124。
  - **情况3**：X ≤ 9 < Y（一人个位数，一人两位数） → 组合成绩 = Y × 10 + X（三位数）。例如 4和12 → 12×10+4=124。
  - 两人杆数相等时，适用对应情况（如4和4→44；10和10→110）。
- **翻牌规则**（双向）：
  - 胜队或负队中若有球员在本洞打出小鸟（Birdie）或更好成绩（基于调整后杆数），则该队获得一次翻牌权，可对**对方队伍**的组合数字进行翻转。翻转操作：交换数字的十位和个位，高位（百位、千位）不变。
    - 两位数：45 → 54。
    - 三位数：130 → 103；124 → 142。
    - 四位数：1110 → 1101。
  - 顺序：若双方都有鸟，则先由胜队翻负队，再由负队翻胜队（如果负队有鸟）。
  - 执行原则：仅当翻转后的数字对执行方有利时才执行。
    - 胜队翻负队：希望负队数字变大，故仅当新数字 > 原数字时执行。
    - 负队翻胜队：希望胜队数字变小，故仅当新数字 < 原数字时执行。
- **比较与积分分配**：
  - 两队比较最终组合数字，数字小者胜。
  - 分差 D = |胜队数字 - 负队数字|。
  - 胜队每人获得 D × K 积分，负队每人支付 D × K 积分（K为积分系数，游戏实例中配置，默认1）。
- **顶洞定义**：用户可选三种方式：
  - 得分打平：两队组合数字相等。
  - 分差≤N：两队组合数字之差的绝对值 ≤ N（N可配置，默认2）。
  - 无顶洞。
  - 顶洞时：本洞团队得分为0，每人积分0，累积1块肉（跨洞累积），下一洞排序不变（若为乱拉/高手不见面）。
- **吃肉**：
  - 非顶洞时，胜队根据其**队内个人最好成绩**（两人中较低杆数）对应的成绩类别，查“成绩-吃肉数量对应表”决定吃肉数量（0~3或“全部”）。
  - 从累积肉池中取出相应数量的肉块，若不足则取全部。
  - 每块肉价值按“肉的分值类型”计算：
    - **肉算N分**：固定 N 分（默认10，可配置）。
    - **分值翻倍**：每块肉价值 = 本洞胜队每人所得积分 D × 2。
  - 肉的总价值加到胜队团队得分上，负队团队得分相应减少。
- **组合分数封顶**：可选启用封顶（默认99）。若启用，任何组合数字（包括翻牌后）若超过封顶值，则取封顶值。

#### 规则模板参数（保存在“我的规则”中）

| 参数            | 说明                                      | 默认值                                |
|:------------- |:--------------------------------------- |:---------------------------------- |
| **顶洞定义**      | 得分打平 / 分差≤N（N默认值=2） / 无顶洞               | 得分打平                               |
| **顶洞时是否重新排序** | 是 / 否（即顶洞时是否根据成绩调整组合）                   | 否                                  |
| 成绩-吃肉数量对应表    | 成绩类别（比鸟更好、鸟、PAR、比PAR更差）→ 吃肉数量（0~3或“全部”） | 比鸟更好=全部，鸟=2，PAR=1，比PAR更差=0（用户可自定义） |
| 肉的分值类型        | 肉算N分 / 分值翻倍                             | 肉算N分（N=10）                         |
| 组合分数封顶        | 不封顶 / 封顶至N（N默认99）                       | 不封顶                                |

#### 游戏实例配置项（创建具体游戏时填写）

| 配置项                     | 说明                                                    | 默认值     |
|:----------------------- |:----------------------------------------------------- |:------- |
| 游戏名称                    | 自定义标识                                                 | “拉斯维加斯” |
| **积分系数 K**              | 每分对应积分（最终积分 = D × K）                                  | 1       |
| **选择球员**                | 从本组球员列表中勾选四人                                          | —       |
| **有效洞范围**               | 基于全程洞序的起始/结束索引                                        | 全洞      |
| **编队方式**                | 固拉 / 乱拉 / 高手不见面                                       | 固拉      |
| **初始排序方式**（乱拉或高手不见面时显示） | 随机 / 手工指定 / 按差点排序（低到高）                                | 随机      |
| **排序规则**（乱拉或高手不见面时显示）   | 与四人拉丝相同（四个选项，基于真实杆数或调整后杆数），拉斯维加斯中让杆为整数，选项3/4可用。默认选项1。 | 选项1     |
| **个人让杆值**               | 为每位球员分别设置按PAR3/4/5的**整数**让杆值（可负）                      | 0       |

#### 各分组方式详解（同拉丝，略）

#### 每洞计算流程

1. 根据编队方式确定本洞的A队和B队。
2. 对每位球员应用个人让杆（整数），得到调整后杆数。
3. 计算每队的组合成绩（根据规则）。
4. 判断是否有小鸟及以上成绩（基于调整后杆数），记录翻牌权。
5. 比较两队初始组合成绩，确定胜队和分差。
6. 执行翻牌（按顺序）：
   - 若胜队有翻牌权，计算翻转后的负队数字，若新数字 > 原数字，则更新负队数字。
   - 若负队有翻牌权，计算翻转后的胜队数字，若新数字 < 原数字，则更新胜队数字。
7. 重新计算分差 D = |胜队最终数字 - 负队最终数字|。
8. 若启用组合分数封顶，将两队数字限制在封顶值内（翻牌后也需检查）。
9. **顶洞判定**：根据规则模板中的顶洞定义判断是否顶洞：
   - 若定义为“得分打平”，则判断两队最终数字是否相等。
   - 若定义为“分差≤N”，则判断 D ≤ N，此时积分仍正常计算，**累积一块肉**。
   - 若定义为“无顶洞”，则不触发顶洞。
   - 若满足顶洞条件：本洞团队得分为0，每人积分0，累积1块肉，下一洞排序不变；否则进入正常分配。
10. 正常分配（非顶洞）：
    - 基础分配：胜队每人 +D·K，负队每人 -D·K。
    - 若累积肉块数 > 0，胜队根据其队内最好成绩（最低杆数）查表吃肉，每块肉价值按配置计算，加到胜队团队得分上（负队相应减少）。
    - 更新累积肉块（减去吃掉的肉块数）。
11. 存储每洞积分。

#### 算法示例

**实例配置**：K=1，固拉，初始排序 [A, B, C, D]。让杆全0，顶洞定义=分差≤N（N=2），肉分值=肉算N分（N=10），吃肉数量表默认，封顶不启用。

#### 第1洞（分差>2，非顶洞）

- 杆数：A=4, B=5, C=5, D=4 → 组合44 vs 55，分差11>2，非顶洞。
- 无鸟，无翻牌。
- 基础积分：A=+11，D=+11，B=-11，C=-11。

#### 第2洞（分差≤2，顶洞）

- 杆数：A=4, B=4, C=4, D=5 → 组合44 vs 44? B队（B,C）=4,4→44，A队（A,D）=4,5→45，分差1≤2，顶洞。
- 本洞积分1，累积肉块+1。

#### 第3洞（吃肉）

- 累积肉块=1，杆数：A=4, B=5, C=5, D=4 → 分差11>2，非顶洞。
- 基础积分：A=+11，D=+11，B=-11，C=-11。
- 胜队最好成绩A=4（PAR），吃肉数量=1，肉价值=10。
- 最终：A=+21，D=+21，B=-21，C=-21，累积肉块清零。

**面向用户的解释文案**

- **组队**：四人分成两队，每队两人。根据个人打出的成绩组合成一个团队成绩。数字小者获胜。
- **组合数字**：每洞把两人杆数拼成一个数字：
  - 两人都打≤9杆：小杆*10+大杆（例：4杆和5杆 → 45）。
  - 两人都打≥10杆：小杆*10+大杆（例：10杆和12杆 → 112）。
  - 一人≤9杆、一人≥10杆：**大杆*10+小杆**（例：4杆和12杆 → 124）。
- **翻牌**：如果胜队有人打小鸟，可以**翻转对手的数字**（交换十位个位，如45→54），让对方变大，自己赢更多。如果负队有人打小鸟，也可以**翻转胜队的数字**（如34→43），让对手数字变大，减少损失。

#### 4.3.6 四人SKINS

#### 玩法说明

SKINS是一种**多人逐洞比洞赛**，每洞，四名球员每人预先贡献**1个Skin**（价值K积分，K由游戏实例配置）。比较所有球员的**调整后杆数**（实际杆数 - 个人让杆值，支持0.5倍数），杆数**唯一最低**的球员赢得当前**奖金池**中的所有Skin（包括之前累积的所有Skin以及本洞每人贡献的1个Skin）。如果一洞没有唯一最低（即至少两人并列最低，或所有人杆数相同），则该洞无人赢得奖金，每人贡献的1个Skin全部存入奖金池，累积到下一洞。**整场比赛结束后**，若奖金池中仍有剩余Skin，则**均分给所有参与球员**。

- **积分分配**（零和）：
  - 每Skin分值 = K（游戏实例中配置，默认1）。
  - 设奖金池初始为0。
  - **每洞开始前**，所有球员各扣除 K 积分（支付本洞的Skin），同时奖金池增加 P × K（P为人数）。
  - 若本洞产生唯一最低的胜者：
    - 胜者获得当前**奖金池**中的所有积分。
    - 奖金池清零。
  - 若无唯一最低：
    - 无人获得奖金，奖金池继续累积。
- **比赛结束后**：
  - 若奖金池余额 > 0，则将其**均分**给所有 P 名球员（每人获得 奖金池余额 / P 积分），奖金池清零。
  - 由于总贡献为 P × 洞数 × K，且所有赢走的积分加上剩余积分应等于总贡献，剩余部分必能被 P 整除，故每人分得整数积分（当K为整数时；若K为小数，结果可能为小数，系统支持）。
- **支持让杆**（个人按PAR3/4/5配置，支持0.5倍数，可负），属于游戏实例配置。
- **无奖励、无顶洞、无包分、无吃肉**。

#### 游戏实例配置项

| 配置项         | 说明                                 | 默认值     |
|:----------- |:---------------------------------- |:------- |
| 游戏名称        | 自定义标识                              | “SKINS” |
| **每Skin分值** | 每个Skin对应积分（K）                      | 1       |
| **参与人数**    | 默认本组四人全部参与（当本组不足四人时，四人游戏不可选）       | —       |
| **有效洞范围**   | 基于全程洞序的起始/结束索引                     | 全洞      |
| **个人让杆值**   | 为每位球员分别设置按PAR3/4/5的让杆值（支持0.5倍数，可负） | 0       |

#### 每洞计算流程

1. **每洞开始前**，所有球员各扣除 K 积分，奖金池增加 P × K。
2. 计算每人的调整后杆数。
3. 找出最低杆数及人数。
4. 若唯一最低：
   - 胜者获得当前**奖金池**中的所有积分。
   - 奖金池清零。
5. 否则：
   - 奖金池继续累积。
6. 所有洞完成后，若奖金池余额 > 0，则每人获得 余额 / P 积分，奖金池清零。

#### 算法示例（4人，K=1，共2洞）

- **初始**：奖金池=0，每人积分=0。
- **第1洞**：
  - 每人支付1 → 每人-1，奖金池=4。
  - 杆数：A=4, B=4, C=4, D=5 → 无唯一最低。
  - 无人获奖，奖金池保持4。
- **第2洞（最后一洞）**：
  - 每人支付1 → 每人累计：A=-2, B=-2, C=-2, D=-2，奖金池=4+4=8。
  - 杆数：A=4, B=4, C=4, D=4 → 无唯一最低。
  - 无人获奖，奖金池=8。
- **比赛结束**：奖金池余额8，均分给4人，每人+2。
- 最终积分：A=0, B=0, C=0, D=0。

#### 注意事项

- 系统需记录奖金池金额，并每洞更新。
- 有效洞范围可选，支持不连续洞序。
- 无排序、无编队、无顶洞等复杂机制。

### 4.4 多人游戏模板

#### 4.4.1 多人拉丝

#### 玩法说明

多人拉丝是一种多人队际赛，参与人数 **N ≥ 6。每洞根据当前排序将球员分为 A、B 两队（人数相等），然后进行 1V1 比洞对抗。支持两种分队模式：**所有一起**（直接按排序分队）和**高手不见面**（按差点分组后交叉组队）。可选总杆PK，支持乘法奖励、顶洞、吃肉等机制。

- **无让杆**，所有比较基于**真实杆数**。
- **轮空处理**：若 N 为奇数，则每洞排序最后一位球员轮空（不参与PK，本洞积分为0，只记录杆数用于下一洞排序）。实际参与PK人数 M = N（偶）或 N-1（奇），M 必为偶数。
- **排序更新**：支持“固定”（首洞后不再重排）和“动态”（每洞后按成绩重新排序）。
- **排序规则**（仅当排序更新为“动态”时生效）：当两人本洞杆数相同时，按以下规则决定顺序：
  - **按出身成绩排序**：比较上一洞的真实杆数，杆数低的排前；若相同，继续向前追溯。
  - **按本洞输赢排序**：比较本洞的**个人积分**（赢为正，输为负，平为0），积分高的排前；若相同，则比较上一洞的个人积分，积分高的排前；若仍相同，继续向前追溯。
- **编队规则**：用于将排序后的球员分配到 A、B 两队，支持两种：
  - **江湖编队规则**：将排序后的球员每4人一组，组内第1、4入A队，第2、3入B队，组间队别交替；最后一组若为2人，则靠后者入A队。
  - **海岛编队规则**：前4名按第1、4入A队，第2、3入B队；剩余球员按奇偶位置分配（偶数位置入A，奇数入B）。

#### 游戏实例配置参数

| 配置项                  | 说明                        | 默认值                  |
|:-------------------- |:------------------------- |:-------------------- |
| 游戏名称                 | 自定义标识                     | “多人拉丝”               |
| 参与球员                 | 从比赛球员列表中勾选 N 人（N ≥ 6）     | —                    |
| 有效洞范围                | 基于全程洞序的起始/结束索引            | 全洞                   |
| 排序更新                 | 固定 / 动态                   | 动态                   |
| **排序规则**（排序更新=动态时显示） | 按出身成绩排序 / 按本洞输赢排序         | 按出身成绩排序              |
| **编队规则**             | 江湖编队规则 / 海岛编队规则           | 江湖编队规则               |
| 分档模式                 | 所有一起 / 高手不见面              | 所有一起                 |
| 初始排序                 | 随机 / 手工指定（拖拽顺序）           | 随机                   |
| 1V1积分系数列表 K_i        | 每个配对的系数，长度 = M/2，可单独设置浮点数 | 全1                   |
| 总杆PK系数 Kt            | 整数或浮点数，0表示不启用             | 0                    |
| 奖励开关                 | 启用/禁用乘法奖励                 | 启用                   |
| 奖励倍数表                | 小鸟、老鹰、信天翁/HIO 对应的倍数       | 小鸟×2，老鹰×5，信天翁/HIO×10 |
| 顶洞定义                 | 无顶洞 / 所有PK打平              | 无顶洞                  |
| 高手不见面专用配置            | 见下文                       | —                    |

#### 编队规则详解

##### 江湖编队规则

**核心思想**：每4人一组，组内按特定模式分配，组间队别交替，确保每队实力均衡且避免强强联手。

**步骤**：

1. 将球员按当前排序从左到右依次编号为 1, 2, 3, …, K（K为组内人数）。
2. 每4人一组，最后一组可能不足4人（实际中K为偶数，剩余人数只能为2）。
3. 对每个四人组（组内球员为 p1, p2, p3, p4，按排序顺序）：
   - **奇数编号组**（第1组、第3组、第5组…）：A队 = {p1, p4}，B队 = {p2, p3}。
   - **偶数编号组**（第2组、第4组、第6组…）：A队 = {p2, p3}，B队 = {p1, p4}。
4. 当最后一组只有2人（即剩余人数为2）时：
   - 设这两名球员为 p1, p2（p1排序在前）。
   - 分配规则：A队 = {p2}，B队 = {p1}。
5. 将所有组的A队成员合并，按原组内顺序（先低编号组后高编号组，组内按原排序）形成最终的A队列表；同样合并B队。
6. 配对时，将A队和B队按各自列表的顺序一一对应（A1 vs B1, A2 vs B2, …）。

**示例（8人）**：

- 排序：1,2,3,4,5,6,7,8
- 第1组(1-4)：奇数 → A={1,4}, B={2,3}
- 第2组(5-8)：偶数 → A={6,7}, B={5,8}
- 最终A队 = [1,4,6,7]，B队 = [2,3,5,8]
- 配对：1vs2, 4vs3, 6vs5, 7vs8

**示例（6人）**：

- 排序：1,2,3,4,5,6
- 第1组(1-4)：奇数 → A={1,4}, B={2,3}
- 第2组(5-6)：2人组 → A={6}, B={5}
- 最终A队 = [1,4,6]，B队 = [2,3,5]
- 配对：1vs2, 4vs3, 6vs5

##### 海岛编队规则

**核心思想**：优先处理前4名，通过固定模式分配；剩余球员按奇偶位置简单分配，使得高低水平球员混合。

**步骤**：

1. 处理前4名球员（排序第1、2、3、4）：
   - A队：第1名、第4名
   - B队：第2名、第3名
2. 处理剩余球员（第5名及以后）：
   - 将剩余球员按原始排序位置编号（第5、6、7, …）。
   - **偶数位置**（第6、8、10…）的球员归入 A队。
   - **奇数位置**（第5、7、9…）的球员归入 B队。
3. 最终A队和B队内部顺序按原排序顺序（先前4名中分配的人，后剩余球员中分配的人，各自保持原序）。
4. 配对：将A队和B队按各自列表的顺序一一对应。

**示例（8人）**：

- 排序：1,2,3,4,5,6,7,8
- 前4名：A={1,4}, B={2,3}
- 剩余(5-8)：5(奇)→B, 6(偶)→A, 7(奇)→B, 8(偶)→A
- 最终A队 = [1,4,6,8]，B队 = [2,3,5,7]
- 配对：1vs2, 4vs3, 6vs5, 8vs7

**示例（6人）**：

- 排序：1,2,3,4,5,6
- 前4名：A={1,4}, B={2,3}
- 剩余(5-6)：5(奇)→B, 6(偶)→A
- 最终A队 = [1,4,6]，B队 = [2,3,5]
- 配对：1vs2, 4vs3, 6vs5

#### 分档模式详解

##### 所有一起模式

- 将所有参与PK的 M 名球员按当前排序，使用选定的**编队规则**直接分成 A、B 两队，然后顺序配对进行1V1比洞。

##### 高手不见面模式

**配置阶段**：

- 用户输入**低差点有效人数 L**（偶数，2 ≤ L ≤ M）。
- 系统自动计算高差点有效人数 H = M - L（H 为偶数），用户可手工调整（需 L+H = M）。
- 用户手动将球员分配到“低差点组”和“高差点组”两个集合（每个集合实际人数 ≥ 对应有效人数）。低差点组通常为差点较低的球员。

**每洞处理**：

1. 从低差点组中取排序最靠前的 L 人，从高差点组中取排序最靠前的 H 人作为实际PK成员，其余球员轮空（本洞积分为0）。
2. 分别对低差点组的 L 人和高差点组的 H 人应用相同的**编队规则**，得到 A_low/B_low 和 A_high/B_high。
3. 交叉合并：最终 A 队 = A_low ∪ B_high，B 队 = B_low ∪ A_high。
4. 将 A、B 队按内部顺序（先低差点后高差点，保持原顺序）排列，然后顺序配对进行1V1比洞。

#### 每洞积分计算

1. **1V1 比洞**：每对比较真实杆数，杆数低者胜，高者负，平局0。胜者得分 +K_i，负者得分 -K_i。
   - **乘法奖励**：若胜者本洞成绩（相对标准杆）达到小鸟、老鹰等，则胜者得分乘以对应倍数，负者得分乘以相同倍数（保持零和）。
2. **总杆PK**（若 Kt ≠ 0）：计算两队本洞总杆（真实杆数之和，轮空球员不计入），总杆低者胜，高者负，平局0。胜队团队得分 +Kt，负队 -Kt。
3. **团队总分** = 1V1 得分之和 + 总杆PK得分。
4. **个人积分**：参与PK的队员每人获得团队总分；轮空球员得0。

#### 顶洞与吃肉

- **顶洞定义**：所有1V1对决均为平局，且若总杆PK启用则两队总杆也相等。
- **顶洞时**：
  - 本洞所有球员积分为0。
  - 累积1块肉（跨洞累积）。
  - 下一洞排序保持不变（动态模式下）。
- **吃肉**（非顶洞且累积肉块数 > 0）：
  - 消耗1块肉。
  - 将本洞**胜队的团队总分**（基础输赢分差）翻倍。
  - 例如：基础团队总分 = +D（胜队），-D（负队）；吃肉后变为 +2D，-2D。
  - 每洞仅触发一次吃肉（即使有多块肉也只消耗一块，翻倍一次）。

#### 其他

- 支持全局捐锅规则（可关联）。
- 支持有效洞范围、手工编辑。

------

## 6. 界面与交互

本章仅记录已确认的产品展示原则。完整界面规范后续单独完善。第 5 章（游戏实例配置清单）与第 7 章（数据结构）目录保留，正文后续专门编写。

### 6.1 三人动态玩法的逐洞阵营识别

对于会随洞重新分组的三人玩法（斗大地主、斗二地主、斗小地主、三人8421 的乱斗），逐洞结果必须让用户能够识别：

- 本洞谁是**单人队**
- 本洞谁是**双人队**

显示内容应对应**该洞实际参与结算的阵营**，而不是下一洞调整后的阵营。顶洞洞同样展示本洞结算时的单/双身份；下一洞身份由下一洞自己的结果表达。

可采用紧凑文案，例如：

`单 张三 · 双 李四/王五`

红蓝等视觉标识可以作为辅助，但不能要求用户仅靠颜色推断“单 / 双”（不同三人玩法中单人队对应的颜色并不相同）。

#### 算法示例（6人，所有一起模式，动态排序，江湖编队规则，K_i=1，Kt=0，无顶洞）

- 初始排序（随机）：[A, B, C, D, E, F]（A排第1，B第2，…，F第6）
- **第1洞**杆数：A=4, B=5, C=5, D=6, E=4, F=5
  - 江湖编队（6人）：A队 = [A, D, F]，B队 = [B, C, E]（详见前例）
  - 配对：A vs B, D vs C, F vs E
  - 对决结果：A胜B (+1), D负C (-1), F负E (-1) → 1V1总分：A队= -1，B队= +1
  - 团队总分：A队=-1，B队=+1
  - 每人积分：A=-1, D=-1, F=-1, B=+1, C=+1, E=+1
  - 动态排序：按杆数重排（杆数低在前）：
    - 杆数：A=4, E=4, B=5, C=5, F=5, D=6 → 排序：[A, E, B, C, F, D]（平局按上一洞排名，A在E前，B/C/F按原顺序）
- **第2洞**（使用新排序）杆数：A=3, E=4, B=5, C=5, F=5, D=6
  - 江湖编队（新排序）：A队 = [A, B?, 按新排序计算具体略]
  - 结果略，同理可得积分。
- **顶洞示例**：假设第3洞所有杆数相同，则所有对决平局，本洞积分0，累积肉块=1。
- **吃肉示例**：第4洞非顶洞，胜队团队总分原为+2，因累积肉=1，翻倍为+4，每人+4，负队-4，肉清零。

#### 4.4.2 喇叭花

#### 玩法说明

喇叭花是一种混合游戏，参与人数  N ≥ 5。每洞，根据当前排序处理如下：

- **轮空处理**：若 N 为偶数，则排序最后一位球员本洞**轮空**（不参与任何对决，个人积分为0，只记录杆数用于下一洞排序）。实际参与对决人数 M = N（奇）或 N-1（偶），M 必为奇数。
- **喇叭花定位**：从参与对决的 M 名球员中，按当前排序找出中间位置（第 (M+1)/2 名）作为**喇叭花**。
- **喇叭花部分**：喇叭花与其余每一位球员进行 1V1 比洞（共 M-1 场对决），所有对决使用统一的积分系数 **K_flower**，统一使用实例配置中的奖励设置。
- **多人拉丝部分**：除去喇叭花后，剩余 M-1 名球员（偶数）自动组成一个**多人拉丝**子游戏，按照本游戏配置的编队规则、1V1积分系数列表、总杆PK系数、统一使用实例配置中的乘法奖励等规则进行计算。且**仅使用“所有一起”模式**（无分档配置）。
- **无让杆**，所有比较基于**真实杆数**。
- **排序更新**：支持“固定”（首洞后不再重排）和“动态”（每洞后按所有球员的本洞杆数重新排序，杆数低者在前，相同按排序规则处理）。
- **排序规则**（仅当排序更新为“动态”时生效）：当两人本洞杆数相同时，按以下规则决定顺序：
  - **按出身成绩排序**：比较上一洞的真实杆数，杆数低的排前；若相同，继续向前追溯。
  - **按本洞输赢排序**：比较本洞的**个人积分**（赢为正，输为负，平为0），积分高的排前；若相同，则比较上一洞的个人积分，积分高的排前；若仍相同，继续向前追溯。
- **顶洞定义**：**所有参与对决的球员（即非轮空球员）本洞杆数完全相同**。此时：
  - 喇叭花部分和多人拉丝部分均不产生积分（所有球员积分为0）。
  - **累积1块肉**（跨洞累积）。
  - 下一洞排序保持不变（动态模式下）。
- **非顶洞时**：
  - 喇叭花部分计算：每场对决中，胜者得 +K_flower，负者得 -K_flower，平局0。若胜者触发乘法奖励，则得分乘以对应倍数，负者同乘。
  - 多人拉丝部分计算：按照配置的编队规则、1V1积分系数列表、总杆PK系数、乘法奖励等计算，得到每人得分。
  - 每位球员的**本洞原始积分** = 喇叭花部分得分 + 多人拉丝部分得分（轮空球员只有多人拉丝部分？轮空球员不参与喇叭花部分，其得分仅来自多人拉丝部分？实际上轮空球员不在参与对决名单中，也不在多人拉丝部分中？注意：轮空球员是单独一人，不参与任何对决，其积分为0。因此轮空球员的原始积分为0）。
- **整体吃肉处理**：
  - 当本洞非顶洞且累积肉块数 > 0 时，消耗1块肉，并将**本洞所有球员（包括轮空球员）的原始积分翻倍**，得到最终积分。
  - 每洞最多消耗1块肉。

#### 游戏实例配置参数（所有配置在同一界面）

| 配置项                  | 说明                                        | 默认值                  |
|:-------------------- |:----------------------------------------- |:-------------------- |
| 游戏名称                 | 自定义标识                                     | “喇叭花”                |
| 参与球员                 | 从比赛球员列表中勾选 N 人（N ≥ 5）                     | —                    |
| 有效洞范围                | 基于全程洞序的起始/结束索引                            | 全洞                   |
| 排序更新                 | 固定 / 动态                                   | 动态                   |
| **排序规则**（排序更新=动态时显示） | 按出身成绩排序 / 按本洞输赢排序                         | 按出身成绩排序              |
| 初始排序                 | 随机 / 手工指定（拖拽顺序）                           | 随机                   |
| **喇叭花对决系数 K_flower** | 每场1V1对决的基础积分                              | 1                    |
| **多人拉丝编队规则**         | 江湖编队规则 / 海岛编队规则                           | 江湖编队规则               |
| **1V1积分系数列表 K_i**    | 每个配对的系数，长度 = (M-1)/2（M = 参与对决人数），可单独设置浮点数 | 全1                   |
| **总杆PK系数 Kt**        | 整数或浮点数，0表示不启用                             | 0                    |
| **乘法奖励开关**           | 启用/禁用                                     | 启用                   |
| **奖励倍数表**            | 小鸟、老鹰、信天翁/HIO 对应的倍数                       | 小鸟×2，老鹰×5，信天翁/HIO×10 |
| **顶洞开关**             | 启用/禁用（参与对决的所有人杆数相同）                       | 启用                   |

#### 每洞计算流程

1. 确定本洞参与对决的球员列表（若 N 为偶数，去掉排序最后一人；否则全数参与）。设实际参与人数为 M（奇数）。
2. 从 M 人中取中间位置（第 (M+1)/2 名）作为喇叭花。
3. 提取喇叭花之外的球员列表（M-1 人，偶数），作为多人拉丝子游戏的参与球员。
4. 计算多人拉丝子游戏得分：
   - 根据编队规则（江湖或海岛），将 M-1 人分成 A、B 两队。
   - 顺序配对进行1V1比洞，每对使用对应的 K_i 系数，胜者 +K_i，负者 -K_i，平局0。
   - 若启用乘法奖励，胜者成绩（相对标准杆）达到小鸟等时，该对得分乘以倍数。
   - 若 Kt ≠ 0，计算两队总杆，胜队 +Kt，负队 -Kt。
   - 团队总分 = 1V1得分和 + 总杆PK得分。
   - 多人拉丝部分每人得分 = 团队总分（全队相同）。
5. 计算喇叭花部分得分：
   - 喇叭花与每个对手（M-1人）比较杆数，胜+ K_flower，负 - K_flower，平0。
   - 若启用乘法奖励，胜者成绩触发时，该对得分乘以倍数。
   - 喇叭花得分 = Σ(各对决得分)；每个对手得分 = 其与喇叭花对决得分。
6. 计算本洞原始积分：
   - 对于喇叭花：`raw = 喇叭花得分 + 多人拉丝得分`。
   - 对于其他参与对决的球员（非喇叭花）：`raw = 其喇叭花对决得分 + 多人拉丝得分`。
   - 对于轮空球员（若有）：`raw = 0`。
7. 判断是否顶洞：检查所有**参与对决的球员**本洞杆数是否完全相同。
   - 若是：本洞所有球员积分为0，累积1块肉，下一洞排序不变（动态时）。结束。
   - 若否：
     - 若累积肉块数 > 0，则消耗1块肉，最终积分 = `raw × 2`。
     - 否则，最终积分 = `raw`。
8. 动态排序：若启用动态，根据本洞**所有球员**（包括轮空）的杆数重新排序（低在前，平局按排序规则），用于下一洞。

#### 其他

- 支持全局捐锅规则（可关联）。
- 支持有效洞范围、手工编辑。